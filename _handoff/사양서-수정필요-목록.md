# 사양서 수정 필요 목록 — 확정 사양 기준

> **전제** : 목업은 **확정 사양**을 따른다. 아래 항목은 **사양서(v0.6) 쪽이 아직 반영되지 않은 것**이며,
> 목업이 틀린 게 아니라 **사양서를 고쳐야 하는 건**이다.
> 최종 갱신 : 2026-08-27

---

## 1. 확정 사양과 사양서가 다른 것 (사양서 수정 대상)

| # | 항목 | 사양서(v0.6) | **확정 사양 = 목업** | 근거 |
|---|---|---|---|---|
| 1 | **검색 유형별 필터 — 색상** | §5-3 차량번호 필터에 `위치 / **색상** / 차종 / 기간` | 차량번호는 `위치 / 차종 / 기간`. **색상은 텍스트 검색에만** | 2026-08-27 구두 확정 |
| 2 | **필터 공통 규칙** | 유형별로 나열만 되어 있음 | **모든 유형의 기본은 `위치(카메라)` + `기간`**, 그 위에 유형별 필터가 붙는다 | 2026-08-27 구두 확정 |
| 3 | **위치(카메라) 필터 형태** | 카메라 평면 체크박스 목록 | **건물 > 층 > 카메라 폴더 트리**. 운영 카메라가 수천 대라 평면 목록이 성립하지 않음. 검색·폴더 일괄선택·부분선택 표시 포함 | 2026-08-27 구두 확정 |
| 4 | **AI 에이전트 기본 배치** | A(팝업)·B(레이어)·C(도킹) 3형태만 정의, 기본값 미기재 | 우측 GNB `AI` 버튼은 **A(작은 팝업)** 이 기본 | 2026-08-27 구두 확정 |
| 5 | **B안 AI 검색 입력 진입** | 미정의 | 입력창을 **클릭한 것만으로는 전환하지 않고**, 대화를 입력해 **전송한 뒤** AI 검색 화면으로 전환 | 2026-08-27 구두 확정 |

---

## 2. 사양서 자체 오탈자 (문구 수정)

| # | 사양서 표기 | 올바른 표기 | 위치 |
|---|---|---|---|
| 1 | `북마트` | **`북마크`** | LNB 라벨 |
| 2 | `현재 대상와 영상을 사건으로 저장합니다.` | **`대상과`** | 사건 등록 팝업 안내 |
| 3 | `유사 대상별로` 를 `유사 대둘별로` | **`유사 대상별로`** | 검색 결과 설명 |
| 4 | `동일 대상로 추적되는` | **`동일 대상으로`** | 그룹화 설명 |

> ※ 목업은 모두 올바른 표기로 반영되어 있다.

---

## 3. 사양서와 GUI가 서로 다른 것 (둘 중 하나를 맞춰야 함)

| 항목 | 사양서 | GUI 원본 | 목업 채택 |
|---|---|---|---|
| **유사도 Chip 구간** | v0.5 : `85% 이상` / `70~84%` / `70% 미만` **3구간** | 결과 그리드 전체를 픽셀 샘플링하면 `#3fbe7e` · `#e88038` **2색만**, 경계는 90 | **사양서(3구간)** 를 따름 |

→ GUI 시안을 3구간으로 고칠지, 사양서를 2구간으로 내릴지 **결정 필요**.

---

## 4. 사양서에 정의가 없어 목업이 임의 구현한 것 (§17)

확정되면 목업을 맞춘다.

- 하단 상태바 `조치필요` 버튼의 동작
- 지능형 알고리즘 목록 (목업은 임의 10종)
- AI 에이전트 추천 질문 3개가 고정인지 동적인지
- AI 추천 후속 **액션**의 종류 (목업은 안내 토스트만)
- 첨부파일의 용도 (업로드 후 무엇을 하는지)
- 무결성 검증(SHA-256) 주체·시점
- GUID 발급 주체·규칙 ("이미지 등록 시 자동 부여"만 정의)
- 고유 사건번호 채번 규칙
- 설치 구역 목록의 출처 시스템
- 카메라 좌표 저장 방식(상대/절대), 이미지 교체 시 기존 좌표 처리
- 검색 히스토리·최근 검색어 저장 위치(서버/로컬)
- `오탐지 신고` 후 처리 흐름
- 맵 미등록 상태에서 맵뷰어·이동 경로 동작

---

## 5. 반영 이력

이 목록의 1·2번 항목은 모두 목업에 반영 완료(2026-08-27).
관련 커밋은 `git log` 의 `사양 §…` / `검색 유형별 필터 구성 정리` 참조.

---

## 6. 2026-08-31 개편 — 제가 판단해 정한 것

사용자가 자리를 비운 사이 진행하며, 사양에 정의가 없어 **제 판단으로 결정**한 항목이다.
확인 후 다르면 되돌리면 된다.

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| 1 | **관심인물 알림** | 알림 **버튼(수신 UI)만** 제거하고 관심인물 등록·관리와 알림 설정 화면은 유지 | 지시는 '알림 버튼 삭제' 였고 관심인물 기능 자체를 빼라는 말은 없었다. 최소 변경 |
| 2 | **카메라 `전체` 의 의미** | 빈 배열이 아니라 **전체 카메라를 담는다** | 기존엔 '전체 체크 = 미선택' 이라 '카메라 선택 필수' 규칙과 모순이었다 |
| 3 | **첫 진입 카메라** | 저장값이 없으면 **전체 선택**으로 시작 | 필수 규칙을 지키면서 첫 진입에서 막히지 않게 |
| 4 | **자연어 색상 다중 언급** | 문장의 색상어를 모두 뽑아 상의 색상 필터에 반영, **OR** 로 처리하고 토스트로 알림 | 사용자가 필터에서 바로 고칠 수 있어 되돌리기 쉽다 |
| 5 | **검출 인물 이름 규칙** | `인물 01`, `인물 02` … (2자리 0채움) | 정렬이 자연스럽고 자릿수가 일정 |
| 6 | **최근 검색 노출** | 입력창을 **선택했을 때만** 아래로 펼침 (텍스트·차량 동일) | 상시 노출은 필터를 밀어내고 검색 후에도 남아 방해됐다 |
| 7 | **RE-ID 진입** | 결과 카드 **더블클릭** | 1클릭은 기존대로 미리보기. 클릭 한 번에 큰 팝업이 뜨면 오조작이 잦다 |
| 8 | **클립 1차 호출 수** | 12개, `더보기` 로 추가 | 사양은 '사용자가 설정한 최대 갯수' 인데 설정 UI가 없어 기본값으로 둠 |
| 9 | **분리 상태의 접기 핸들** | 감춘다 | 패널이 떠 있어 핸들을 붙일 가장자리가 없다 |

### 아직 확인이 필요한 것
- **관심인물 알림의 최종 사양** — 위 1번 결정이 맞는지, 기능 자체를 뺄지
- **클립 최대 개수 설정 위치** — 환경설정에 둘지, 팝업 안에 둘지

---

## 2026-09-09 · 오늘 시안 기준 정리 (제거 항목)

기준 : `1-1안 - Solid/요청사항 반영`(GUI) · `검색 메인`(UX 와이어) · `비교-대상4개`(상세)
원칙 : **동작·구성 = 사양서(와이어) / 디자인 = GUI**
방식 : 기능 코드는 남기고 **표시만 끔**(CSS 한 블록). 되살릴 때 그 블록만 지우면 된다.

| 제거 | 자리 | 사유 |
|---|---|---|
| 히트맵 · 경로 확인 · 주변 카메라 · 단일/다중 view · 전체보기 | 상세 재생바(dtTools) | 시안 재생바는 재생 컨트롤 + 설정(톱니) 뿐. 분석은 타일 아이콘·맵 도구가 맡는다 |
| 영역 검색 · 선 검색 | 상세 재생바 좌측(dtAreaTools) | 타임라인 하단 편집도구와 중복 |
| 편집줄 | 재생바 아래(dtEditBar) | 시안은 타임라인 하단 한 곳에만 둔다 |
| 패널 분석 도구 · 영상/맵뷰 전환 | 상단 패널(pn-tools, pn-switch) | 좌측은 영상 전용, 맵은 우측 칼럼 |
| 지도 / 층별 세그 | 맵 상단(dtMapSeg) | 시안은 `A동 (1건)` **동 선택**으로 대체 → 신규 추가 |
| 경로 비교 바 | 결과 하단(compareBar) | 상세의 `인물 추가` 로 대체 |
| 대상정보 카드 · 1단/2단/3단 · 타임라인 상단 탭 | 상세 | 시안에 없음. 트랙 라벨(썸네일+이름)이 대신 |
| 분리(디테치) · 패널 가장자리 접기 핸들 | 검색 패널 | 시안은 머리말 접기 버튼 하나 |
| 색상 필터 | 검색 패널 | 1-1안에 없음 (단, 1안·2안·4안에는 있음 — 확인 필요) |

### 확인이 필요한 충돌
- **색상 필터** : 1-1안에는 없고 다른 GUI 변형(1안·2안·4안)과 와이어에는 있다. 현재 1-1안 기준으로 제거.
- **접힘 펼침 버튼** : 와이어(기본/최소화)는 가장자리 핸들, 구두 지시는 `검색 홈` 탭 우측. **구두 지시**를 따랐다.
- **크기 슬라이더·보기 모드** : 1-1안에는 없고 와이어에는 있다. 와이어(구성)를 따라 되살렸다.

## 2026-09-11 · 검색 상세의 검색결과 패널 호출 (구두 확정)

| 항목 | 확정 사양 | 비고 |
|---|---|---|
| 검색 상세의 `검색 결과` 탭 | 목록으로 돌아가는 탭으로만 동작. **검색결과 패널을 불러오는 펼침 버튼은 두지 않는다** | 펼침 버튼은 검색 화면에서 패널을 접었을 때만 탭 우측에 붙는다 |

## 2026-09-11 · 상세 맵뷰어 / 다층 뷰(3D) — 새 시안(5184:77047) · 동작 사양서(4759:59565) 반영

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 맵 기본 표시 | **평면 단일 층**. 커서가 있는 층의 도면·지점만 보이고, 커서가 층을 옮기면 도면도 바뀜 | 시안 `검색 상세_기본`(1층 도면) · 사양 4-3 |
| 다층 뷰 | 우측 도구 `다층 뷰` 버튼으로 전환. three.js 로 층을 실제 3D 로 쌓고 드래그 360° 회전 · 휠/+− 줌 · 더블클릭 초기화 | 시안 `지도 다층뷰`(5184:100653) |
| 다층 뷰 층 구성 | **대상이 포착된 층만** 쌓음. 층 태그는 각 층 **우측** | 사양 PTS_DTL_MAP_000_1 (1). 시안에는 빈 가운데 층이 그려져 있어 **충돌** — 사양을 따름 |
| 경로선 | 같은 층·같은 구역은 실선, 층간·외부 이동은 점선 | 사양 4-4 |
| 3D 조작 안내 | 시안 제안 3안을 모두 구현하고 **주소 뒤 `?m3d=1|2|3`**으로 선택 (기본 ①, 설정 삭제로 옮김) — ① 호버 툴팁(3초 후 사라짐) ② 중앙 하단 상시 안내 ③ 우측 하단 3D 버튼(눌러야 회전) | 시안 노트가 세 가지 '제안'이라 확정안 선택 필요 |
| 맵 도구 구성 | 다층 뷰 │ 주변 카메라 표시 · 이동 경로 리스트 (+ 확대/축소). `전체보기`는 도구에서 뺌 | 시안 · 사양 4-3 |
| 주변 카메라 원뿔 | 평면에서만 표시 (3D 층판 좌표와 맞지 않음) | 3D 위 카메라 표시는 사양 미정 |

## 2026-09-11 · 설정 삭제 · 상세 LNB 미노출 · 3단계 반영

| 항목 | 구현 | 근거 |
|---|---|---|
| GNB `설정` | **삭제** (설정 안의 메뉴가 사양에서 모두 빠짐, 재생 설정은 영상 설정 팝오버로) | 구두 확정 |
| 상세 화면 GNB | **상세에서도 GNB 표시** (구두 확정). 사양서 “상세화면 진입 시 LNB 미노출”과 **충돌** — 구두 지시를 따름 | 2026-09-11 구두 |
| 재생 속도 목록 | 0.25/0.5/1.0/1.25/1.5/2.0 (0.75 제외) | 사양 2-7. 시안 팝오버에는 0.75 가 있어 **충돌** — 사양을 따름 |
| 경로 편집 / 영상 편집 아이콘 | 분할 아이콘 = 경로 편집(영상 선택 팝업), ✎ = 영상 편집(구간 핸들, 하단 취소·저장) | 시안 `영상 클립 편집`에서 ✎ 활성 |
| 인물 선택 팝업 | 화면에 인물 2명 이상일 때 경로 편집 전에 표시 | 사양 경로 편집 3 |

## 2026-09-11 · 맵 구역 모델 (A동 / 외부) · 멀티 뷰 · 영역 전환

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 구역 구분 | 카메라명에 `외부`가 있으면 외부 구역, 그 외 A동(건물) | 샘플 데이터 규칙 — 실데이터는 카메라의 구역 속성으로 대체 필요 |
| A동 지도 | 도면(평면)·다층 뷰에는 **외부 지점을 표시하지 않음** | 사양 (1)(2) |
| 외부 구역 뷰 | 외곽 지도(map.png) · 원본 비율 유지 레터박스 · 휠/± 확대 · 확대 시 드래그 이동 · 34px 안 지점 묶음 배지(누르면 그 지점 중심 2배 확대) · 다층 뷰 비활성 | 사양 (2) 2-1 · 2-2 |
| 구역 자동 전환 | 커서가 외부 구간에 들어가면 외곽 지도로 자동 전환(층 자동 전환과 동일). 드롭다운·`구역 이동 ›`으로 직접 옮기면 자동 전환을 멈추고, 지도 지점·이동 경로 리스트로 이동하면 다시 따라감 | 사양 4 “타임라인 연동” · 4-2 “타 구역 이동 네비게이션” 해석 |
| 멀티 뷰 | 둘째 창 = 다음 탐지 구역(없으면 직전), 창마다 구역 드롭다운, 다른 창에 떠 있는 구역 비활성, 구역이 하나뿐이면 버튼 비활성 | 사양 (3) |
| 영역 전환 | 맵 섹션을 영상 자리(넓은 왼쪽)로, 영상을 오른쪽 칸으로 실제 이동. 멀티 뷰는 좌우 배치, 영상은 레터박스 | 사양 4-1 · 시안 맵뷰어듀얼 |
| 샘플 데이터 | 외부 구간을 보이려고 인물 A 여정 끝에 외부 카메라 3개(본관 별동·주차장 1구역·사업장 정문)를 **타임라인·지도 동일하게** 추가 | 목업 시연용 |

## 2026-09-11 · 타임라인 트랙 · 경로 비교 · 2D 맵 확대

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 트랙 썸네일 | 컬러바 8→4px, 썸네일 57→44px(행 50px). 클립마다 **해당 카메라 원본 프레임**을 채움(전부 같은 프레임이던 문제) | 구두 지적 · 시안 |
| `경로 비교` 버튼 | 트랙 한 줄 추가 → **경로 비교 추가 팝업**(사양 2-2): 날짜별 대상 인물 목록, 분석 중인 인물 체크 고정, 최대 3명 추가(총 4명, 차면 나머지 비활성), `추가`는 1명 이상 선택 시 활성 → 고른 인물 그대로 비교 화면(슬롯 색·이름·트랙 일치) | 사양 2-2 |
| 비교 탭에서 다시 추가 | 비교 탭을 새로 쌓지 않고 **현재 비교 탭을 교체** | 사양 미기재 — 탭 누적 방지 |
| 트랙 칩 ✕ | §14 공통 Alert `인물 삭제`(“선택한 인물이 삭제됩니다…”) 확인 후 삭제, 기준 인물(1번)은 삭제 불가, 1명 남으면 단일 뷰로 | 사양 2-1은 “인물 삭제 확인”으로만 표기 — 문구는 §14 공통 Alert 를 따름 |
| 경로 비교 대상 목록 | 목업은 샘플 트랙 4명(같은 날짜 한 묶음)만 | 실데이터는 최근 검색·히스토리의 동선 생성 완료 인물 |
| 2D 평면 ± 확대 | 평면 도면에서도 ±·휠 확대(1~4배), 확대 시 드래그 이동. ± 버튼을 상단 도구 아이콘과 좌우 정렬 | 구두 지적 |

## 2026-09-11 · 시안 5184:77047 정밀 맞춤 (타임라인 · 툴바 · 영상 영역)

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 타임라인 규격 | 눈금 48(날짜 20 + 시각 28) · 트랙 행 65(막대 8 + 썸네일 57) · 행 간격 2 · 인물 칩 80×65 · 하단 컨트롤 51. 상세 1명은 높이 206 고정(시안), 비교는 인원수만큼 늘어남(3명 308, 4명 375) | 시안 5184:99014 · 100218 실측 |
| 구간 막대 두께 | **시안 8px 로 되돌림.** 직전 구두 지적(“컬러바 두꺼움”)으로 4px 로 줄였으나, “시안 기준으로 전체 맞춤” 지시에 따라 시안 값 적용. 막대와 썸네일 테두리가 같은 색 한 덩어리(탭 모양)로 붙도록 바꿔 떠 보이던 문제 해소 | 두꺼우면 재조정 필요 — 구두 확인 대기 |
| 트랙 썸네일 | 100×56 프레임을 1px 간격으로 반복(시안), 번호는 원형 16 · 카메라 수는 반투명 알약 | 시안 |
| 인물 칩 줄 · 편집 버튼 | 타임라인 위 `전체 / 인물 A… / 인물 비교` 줄과 `편집` 버튼 **삭제**(상세·비교 모두). 편집은 하단 ✎ | 시안 전 화면에 없음 |
| 비교 화면 툴바 | 상세와 같은 구성. 영역·선 검색 · 히트맵 · 주변 카메라 · 뷰 전환은 20% 비활성, 재생·설정만 활성. 남아 있던 `경로 확인 · 다중 영상 · 전체보기` 와 별도 `일시정지` 버튼 삭제(재생 토글) | 시안 5184:100217 |
| 재생 버튼 순서 | 이전 · 배속뒤로 · 10초뒤로 · 재생(토글) · 10초앞 · 배속앞 · 다음 — 북마크 상세 등 공용 컨트롤도 동일하게 | 시안 5184:98968 |
| 상세 영상 머리말 | dim 80 · 위치 12M + 일시 12R · 아이콘 24(간격 16). 머리말의 `인물 A` 라벨은 시안에 없어 숨김 | 시안 5184:98967 |
| 경로 비교 영상 배치 | 2명 2열 · **3명 3열**(빈 칸 없음) · 4명 2×2. 2·3명은 칸 안에서 원본 비율(레터박스) + 동시 포착 카메라 3대 상시 노출(2명 우하단 · 3명 좌하단), 4명은 `동시 포착 (3)` 버튼 → 팝오버 | 시안 100188 · 100209 · 100230 |
| 비교 타일 머리말 | 인물 라벨(반투명 칩 · 색 점) · 위치 · 일시. 2명은 북마크·비율·전체보기, 3·4명은 ⋯ | 시안 |
| 대상 바운딩 박스 | 시안 비교 화면엔 박스가 없지만 **사양 2-2(상시 표시)** 에 따라 유지 | 사양 우선(동작·구성) |
| 타임라인 가로 스크롤 | 3~4명일 때 스크롤바가 마지막 트랙을 가리던 문제 : 기본 배율에선 스크롤이 생기지 않게(맨 끝 다음날 08:00 라벨 제거, 시안도 07:00 까지), 확대 시엔 트랙 아래 10px 스크롤바 자리를 따로 둠 | 구두 지적 |
| 맵 축소 한도 | − 로 원본보다 작게 : 평면 0.5~4배(1.4배씩), 외곽 0.5~6배(1.5배씩), 3D 거리 9~72(기본 27). 원본(1배)을 지나칠 땐 1배에서 한 번 멈춤. 원본보다 작을 땐 가운데 고정·끌기 없음 | 구두 지시 — 최소값은 임의 설정(조정 가능) |
| 영상 조작 바 `설정` | **삭제**(상세·비교). 오른쪽 도구(히트맵·주변 카메라 │ 단일·동시 포착)는 우측 끝으로, 재생 조작은 가운데 고정. 시안 5223:57284 에는 설정이 남아 있어 **충돌** — 구두 지시 따름. 설정 팝오버의 재생 속도·반복 재생·음량은 **진입점이 없어짐** → 옮길 위치 결정 필요 | 2026-09-11 구두 |
| 영상 전체 보기 팝업 | 시안 5223:57487 대로 : 헤더 36(영상 조회 · 닫기) · 16:9 영상(위치·일시 · 북마크·비율) · 6px 재생 막대(버퍼·재생 위치, 눌러서 이동) · 가운데 재생 조작만. 옛 눈금·구간 줄·분석 도구 삭제 | 시안 |
| 타임라인 재생 커서 | 모든 트랙(썸네일·번호 배지·선택된 클립)보다 위에 표시 | 2026-09-11 구두 |
| 인물 상세 툴팁 | **왼쪽 인물 칩 호버에만** 표시(사진 · 인물명 · 전체 구간 · `인물등록 ›` → 관심 인물 등록). 영상 클립 호버 시엔 표시 안 함. 칩 → 툴팁으로 옮겨가는 동안 유지 | 2026-09-11 구두 · 시안 Hover |
| 커서 호버 미리보기 | 재생 커서에 마우스를 올리면 **커서가 가리키는 영상 클립의 썸네일**(160×90 · 트랙색 2px 테두리) + 시각 배지(`HH : MM : SS`). 마우스가 올라간 트랙의 클립 우선, 없으면 그 시각에 클립이 있는 첫 트랙. 클립이 없는 시각이면 시각만. 끄는 동안 커서를 따라감. 커서 옆 옛 시각 배지는 숨김 | 2026-09-11 구두 · 시안 5223:57371 Thumbnail_Hover |
| 타임라인 하단 편집 버튼 | **✎ = 경로 편집(영상 선택·추가) / 필름 아이콘 = 영상 편집(클립 길이)** 로 맞바꿈(앞서 기록한 배정과 반대) | 2026-09-11 구두 |
| 클립 길이 편집 | **줄이기만** 가능 — 처음 탐지 구간 밖으로 늘릴 수 없음(최소 1분) | 2026-09-11 구두 |
| 편집 상태 저장 버튼 | 글자 가운데 정렬 | 구두 지적 |
| 3D 다층 뷰 층 수 | 포착된 층만 쌓던 것을 **건물 층 최대 4개(3층·2층·1층·지하 1층) 모두** 표시. 기본 시점 거리 27→36 | 2026-09-11 구두 |
| 경로 비교 맵뷰 | 비교 인원 수만큼 경로 표시 — 평면·3D·적층 대체 뷰 모두 인물 색으로. 평면은 현재 층의 지점만(사양)이라 샘플 데이터에 인물마다 1층 지점 1개씩 보강(A 1F 엘리베이터 홀 · B 1F 계단 · C 1F 로비 · D 1F 업무공간, 타임라인·지도 동일) | 2026-09-11 구두 · 목업 데이터 |
| 타임라인 가로 스크롤바 | 브라우저 막대 숨김 → **반투명 오버레이 막대**(평소 흐리게, 타임라인 호버·스크롤 시 진하게, 끌기·클릭 이동). 자리를 차지하지 않아 확대/축소로 생기거나 사라져도 높이가 안 변함 | 2026-09-11 구두 |

## 2026-09-11 · 멀티 뷰 · 동시 포착/이동 경로 (사양 PTS_DTL_004_1 재반영)

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 멀티 뷰 버튼 | 그리드 아이콘 = 멀티 뷰 버튼 → 1X1 에서 2X2 **즉시 전환**, 영상 검색(영역·선) 버튼 비활성. 단일 버튼으로 복귀 | 사양 1 |
| 영상 유형 드롭다운 | **동시 포착 / 이동 경로** 두 항목(기본 동시 포착, 선택 항목 ✓). `주변 카메라` · `맵뷰어` 항목 삭제 | 사양 1-1 · 구두(맵뷰어 불필요) |
| 멀티 뷰 내용 | **추적 중인 한 인물**의 영상만. 여러 인물 박스·`주변 카메라 (N)` 버튼·카메라 바꾸기 삭제 | 사양 2 · 구두 지적 |
| 동시 포착 모드 | 좌상단 = 지금 재생 중인 메인 구간, 나머지 = 같은 시간대·인접 구역(같은 층·구역) 타 카메라 최대 3개(같은 장소 추가 카메라 우선), 일시는 메인과 동기화. 부족하면 빈 칸 | 사양 2 |
| 이동 경로 모드 | 타임라인 동선 순서 4개 1세트(#1~#4, #5~#8…), 재생 구간이 다음 세트로 넘어가면 자동 페이지 전환, 재생 중인 칸 강조, 칸 누르면 그 구간으로 이동. 4개 미만이면 빈 칸 | 사양 2. “좌상단 메인 유지”와 “세트 고정 그룹화”가 겹칠 때는 **세트 그룹화**를 따름(메인이 #2 이면 #1~#4 세트에서 #2 칸 강조) — 확인 필요 |
| 칸 머리말 | 카메라명 · 촬영 일시 · 북마크 · 영상 비율 · 전체 보기 | 사양 2-1 · 시안 |
| 전체 보기(멀티 뷰) | 영상 전체보기 팝업에 2×2 그대로 + 재생 막대 · 재생 조작 | 사양 PTS_DTL_001_3 [멀티뷰 선택 시] |
| 멀티 뷰 버튼 형태 | 시안 View Mode 대로 **[단일] [멀티 = 아이콘 · 영상 유형 · ▾] 두 개**로 합침(따로 떼어 둔 그리드 버튼 삭제). 아이콘·글자 = 2X2 즉시 전환, ▾ = 동시 포착 / 이동 경로 목록(✓). 선택된 쪽은 어두운 바탕 + 파란 아이콘·글자 (파란 채움 버튼 아님). 비교 툴바도 동일 | 2026-09-11 구두 · 시안 5223:57284 · 57527 |
| 멀티 뷰 칸 머리말 | 위치 · 일시 … **⋯** 하나(북마크 · 영상 비율 · 전체 보기는 ⋯ 메뉴) | 시안 5223:57527 (사양서 캡처의 아이콘 3개와 다름 — 시안 따름) |

## 2026-09-14 · 인물 칩 활성/비활성 · PIP · 영역 전환

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 인물 칩 활성/비활성 | 경로 비교에서 타임라인 인물 칩을 누르면 토글. 비활성 = 영상 칸 Dim(칸 크기·위치 유지) · 타임라인 줄 Dim · 편집 핸들 미노출 · 지도(평면·3D) 경로 투명도 0.25 · 지점 0.3. ✕(삭제)는 토글 아님. 최소 한 명은 활성으로 남김(안내 토스트) | 사양 4759:35111 · 시안 5223:58528 · 2026-09-14 구두. 최소 1명 유지는 사양 미기재 — 임의 |
| 인물 칩 툴팁 위치 | 칩 **위쪽**에 띄워 칩 오른쪽 위 삭제(✕) 버튼을 가리지 않음 | 2026-09-14 구두 |
| 동시 포착 PIP | 영상 **위에 겹쳐** 우하단 표시(영상 아래 별도 줄 아님). 호버 = 어두운 상자 테두리 + `동시 포착 (N)` + ✕(접기). 접으면 우하단 모서리 `동시 포착 (N) ↘` 버튼, 누르면 펼침. 칸이 좁으면(폭 470 · 높이 230 미만) 처음부터 접힘, 한 번 바꾸면 그 칸 상태 유지. 상세 단일 · 경로 비교 2/3/4명 · 멀티 뷰(이동 경로 칸) 공통. 4명 비교의 팝오버 방식과 칸 아래 카메라 줄은 이것으로 대체 | 2026-09-14 구두 · 시안 5223:57285 · 58374(호버) · 57679(접힘). 호버 제목은 시안에 `주변 카메라 (3)` 도 섞여 있어 `동시 포착` 으로 통일 |
| 영역 전환 대상 | 상세 화면은 영상(#dtVideo), **경로 비교 화면은 비교 영상 묶음(칸 + 조작 바)** 을 오른쪽 칸으로 옮기고 지도를 넓은 자리로. 비교 화면에서 상세 영상을 옮기던 탓에 지도가 사라지고 엉뚱한 단일 영상이 뜨던 문제 수정. 탭을 오가면 조용히 원래 배치로 | 사양 4759:35134 · 2026-09-14 구두 |
| 영역 전환 시 조작 바 | 오른쪽 좁은 칸에는 **재생 조작만** 가운데(영상 검색·분석 도구 미제공). 시안 5223:57627 에는 조작 바 자체가 없어 **충돌** — 사양(동작·구성) 따름 | 사양 4759:35134 2) “영상 검색 도구 및 분석 도구 미제공” |
| 영역 전환 시 멀티 뷰 · 비교 칸 | 위에서 아래로 쌓음 — 멀티 뷰 1×4, 경로 비교 2명 1×2 · 3명 1×3 · 4명 1×4. 3·4명은 칸을 채우고 2명은 원본 비율. PIP 는 시안처럼 접힌 버튼이 기본 | 시안 5223:57627 (기본 · 듀얼 · 3분할 · 4분할) · 2026-09-14 구두 |
| PIP 카메라 선택 | 누르면 **그 영상 칸 안에서** 메인 영상(영상 · 카메라명)과 자리를 바꿈 — 전체 보기 팝업 아님. 원래 메인은 PIP 로 내려감, 다시 그려도 유지, 원래 카메라를 누르면 원상태. 상세 단일 · 경로 비교 칸 · 멀티 뷰(이동 경로) 공통. 상세 단일은 예전에 클립 데이터(카메라명·이미지)를 바꿔 써서 멀티 뷰 카메라명이 뒤섞이던 문제도 함께 수정 | 2026-09-14 구두 |
| 클립 길이 편집 미리보기 | 줄여서 잘려 나간 원래 구간을 흐린 썸네일(점선)로 계속 표시 + 손잡이를 누르거나 끄는 동안 끝 지점 프레임·시각 팝오버(커서 미리보기와 같은 모양), 놓으면 닫힘 | 2026-09-14 구두 |
| 3D 미검출 층 | 인물이 검출되지 않은 층의 판·층 이름을 원본의 50% 투명도로(적층 대체 뷰 포함) | 2026-09-14 구두 |
| 이동경로 리스트 팝업 | 시안 5223:58721 대로 재구성 — `■ 인물명` 머리 · 행 48(번호 원 16 · 위치명 · `시각 • 카메라 코드`) · 층/구역이 바뀌는 곳에 회색 띠. 경로 비교는 인물별 칸(160)을 옆으로 나란히, 칸마다 현재 지점 표시. 뭉개짐 원인 = 행이 지도 전체 보기의 절대 배치 클래스(mvw-wp)를 같이 써서 한자리에 겹침 | 2026-09-14 구두 · 시안 |
| 맵뷰 자리(오른쪽 칸) 영상 조작 바 | 재생 조작을 칸 **정가운데**(3칸 격자), 오른쪽에 단일/멀티 뷰 전환 버튼(좁은 폭이라 아이콘 · ▾ 만). 검색·분석 도구는 계속 미제공 | 2026-09-14 구두 — 앞서 “재생 조작만” 으로 둔 결정을 보완 |

## 2026-09-14 · 검색 홈 와이어프레임(4862:108451) 반영 — 1차 : 검색 패널

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 검색 방식 탭 순서 | 텍스트 · **차량번호 · 인물** · 지능형 알고리즘 | 와이어프레임 |
| 필터 구성·순서 | 텍스트 : 카메라 · 유사도 · 기간 · **색상(상의·하의)** / 차량번호 : 카메라 · **유사도** · 기간 · **색상(차량 한 줄)**, **차종 삭제** / 인물 · 지능형 : 카메라 · 유사도 · 기간 | 와이어프레임. 인물 화면은 유사도 아래가 잘려 기간 유지 여부 불명 — 유지 |
| 기간 선택지 | 당일 → **오늘**, 날짜 미정 → **날짜 지정** | 와이어프레임 배지 `오늘`, 영상 선택 팝업 선택지 |
| 카메라(위치) 배지 | `N개 선택 ✕` — ✕ 로 선택 해제. 5개 이상이면 ⓘ 툴팁 “5개 이상 선택 시 검색 완료까지 다소 시간이 걸릴 수 있습니다.” | 와이어프레임 툴팁 문구 `N개 이상 선택 시…` — **N 값·붙는 위치 미정**, 카메라 배지에 5개로 임의 적용 |
| 등록 인물 | 머리말(등록된 인물 수 · 인물 관리 톱니) 숨김, 카드 ⋮ → 수정 · 삭제. 빈 상태 “등록된 인물이 없습니다. 이미지를 업로드해 검색해 보세요.”(새 인물 등록 버튼 삭제) | 와이어프레임 등록인물o · 등록인물x |
| 이미지 업로드 | “JPG, PNG, BMP 지원 · 최대 20MB”, 검출 인물 태그 전신 → **외형**, 검출된 인물 수 배지 · 항목 + 표시 | 와이어프레임 이미지 default · 이미지_등록 |
| 빈 결과 문구 | 인물 : “인물을 선택해 주세요.” | 와이어프레임 |
| 색상·기간 요약 | 고르면 접이 머리말 요약이 바로 바뀌도록 수정(기존 버그) | — |

## 2026-09-14 · 검색 홈 와이어프레임 — 2차 : 검색 결과

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 결과 더보기 | 건수 옆 `결과 더보기 ⓘ`(툴팁 “더보기 시 순서가 조정될 수 있습니다.”). 누르면 유사도 기준 아래(-15%p) 결과까지 더 불러와 다시 정렬, 한 번 쓰면 비활성, 새 검색 시 초기화 | 와이어프레임. **불러오는 범위는 사양 미정 — 임의** |
| 정렬 선택지 | 텍스트·인물 : 유사도순·최신순·위치순(**인물순 삭제**) / 차량번호 : **최신순·위치순·번호순** / 지능형 : **이름순·최신순·위치순**. 방식을 바꾸면 그 방식 첫 항목이 기본 | 와이어프레임 정렬 드롭다운 |
| 결과 묶음 | 차량번호 = 날짜별 · 인물 = 인물별 · 지능형 = 알고리즘별, 머리말 `▲ 이름 (N)` 눌러 접기/펼치기. 텍스트는 기존(최신순 날짜 · 위치순 위치) | 와이어프레임 |
| 결과 카드 | 차량번호 = 차량번호 + `위치 · 일시`, 인물 = 인물명 + `위치 · 일시`, 지능형 = 알고리즘명 + `위치 · 일시` | 와이어프레임. 차량번호는 목업 값(입력한 숫자 포함) |

## 2026-09-14 · 검색 홈 와이어프레임 — 3차 : 팝업 · 흐름

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 히스토리 팝업 | 제목 `히스토리`(부제 삭제). 날짜별 묶음, 행 = 썸네일 · 인물명 · 카메라 위치 · 📍지점 수 · 일시 · ✕(삭제). **사건 등록 · 북마크 · 연관 검색 펼침 · AI 배지 · 닫힌 탭 목록 삭제**. 행을 누르면 그 인물 상세 탭 | 와이어프레임 4862:108625. 목업 데이터로 인물명·지점 수 생성 |
| 지능형 → 인물 재검색 | 지능형 결과 카드 ⋮ 에 `인물 검색` 추가 → `인물 선택` 팝업(“영상에서 검출된 인물을 검색할 수 있습니다.”, 전체·얼굴·외형, “인물 추출 중”) → 검색 시 **인물 > 이미지 탭으로 전환**해 그 영상 프레임·검출 인물로 재검색 | 와이어프레임 지능형 2·3단계. **진입 버튼 위치는 와이어프레임에 없어 ⋮ 메뉴로 임의** |
| 이미지 검색 결과 묶음 | 고른 검출 인물 수만큼 `인물 01 …` 로 묶음(결과마다 따로 묶이던 문제 수정) | — |
| 영상 선택 팝업 좌측 | 이름 아래에 카메라(`전체 카메라에서 검색`) · 기간(오늘 · 최근 3일 · 최근 7일 · 날짜 지정, 배지) · `재검색` | 와이어프레임 4862:108462. 재검색은 목업(목록 갱신 + 안내) |

## 2026-09-14 · 상세 화면 UI 사양서 v0.8 반영 — 청크 A : 영상 영역 · 공통

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 영상 머리말 버튼 | 북마크 · 영상 비율 · **영상 확대(돋보기)**. 전체보기는 머리말에서 빼고 조작 바 오른쪽 끝(2-6) | v0.8 2-1 · 2-6 |
| 북마크 | 저장 시 토스트 “북마크에 저장되었습니다.”, 다시 누르면 삭제 | v0.8 2-1 |
| 영상 비율 | **원본 비율 · 맞추기 · 자르기**(기본 원본 비율, 선택 항목 ✓) — 옛 화면 맞춤·2x·4x·6x 는 확대로 분리 | v0.8 “비율 설정 / 확대 설정 분리” |
| 영상 확대 | 1.0 · 2x · 4x · 6x 팝오버. 1.0 초과 = 확대 모드 : 재생 멈춤 · 분석/뷰어 도구 비활성 · 휠 한 단계씩(팝오버 연동, 최소 도달 시 1.0) · 드래그 이동 · **좌측 하단 전체 썸네일에 현재 부분 표시(드래그 이동)** · ESC 즉시 1.0 | v0.8 2-1 · PTS_DTL_004_1 확대 모드. 사양의 “드래그로 사각형 지정 후 재실행/검색” 은 영역 검색 문구가 섞인 것으로 보여 **미반영(확인 필요)** |
| 배속 | x1→x2→x4→x8 순환(정/역방향), 현재 배속을 **영상 상단 중앙**에 `x2 ▶▶` / `◀◀ x2` 로 표시(버튼 옆 배지·토스트 제거) | v0.8 2-5 |
| 도구 명칭 | 선 검색 → **가상펜스 검색**, 영상 도구 주변 카메라 → **인접 카메라**, 지도 도구 → **주변 카메라 표시**, PIP 제목 → **주변 카메라 (N)** | v0.8 전반 |
| 인접 카메라 전환 | 영상 위 카메라 아이콘 클릭 → 메인 영상이 그 카메라로 즉시 전환(재생 시점 유지) · 분석/뷰어 도구 비활성 · 머리말 왼쪽 `← 이전 영상` 으로 복귀(전에는 전체보기 팝업) | v0.8 PTS_DTL_003_1 3 |
| 탭 닫기 alert | 탭 ✕ → “탭 닫기” alert(취소 · 닫기, `다시 묻지 않기` 체크 시 이후 묻지 않음). 문구 “사건으로 등록되지 않은 검색 기록은 히스토리에 최근 50개까지 보관되며, 이후에는 다시 확인할 수 없습니다.” | v0.8 공통 팝업 8. 사양 문구의 `00개`·`이 후` 는 **50개(목업 보관 한도)**·`이후` 로 고침 |
| 탭 제목 | `이름 + 카메라 위치명`(예: 인물 A B1 주차장) | v0.8 PTS_DTL_000_1 |
| 대화 목록 삭제 alert | “선택한 대화 목록이 영구적으로 삭제됩니다. 삭제하시겠습니까?” | v0.8 공통 팝업 1 |
| 토스트 | 화면 중앙 하단 · 체크 아이콘 + 닫기 ✕ | v0.8 “토스트 팝업 위치 정의” |
| 인물 상세 진입 시 LNB | 사양은 미노출, **구두 지시(표시) 유지** | 기존 결정 |

## 2026-09-14 · 상세 사양 v0.8 — 청크 B : 멀티 뷰

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 멀티 뷰 드롭다운 | `재생 영상 : 이동 경로 ›`(이동 경로 · 주변 카메라, 기본 이동 경로) / `화면 분할 : 4분할 ›`(2 · 3 · 4분할, 기본 4분할) 2단 메뉴. 버튼 글자 = 현재 재생 영상 | v0.8 PTS_DTL_004_1 1-1. 기존 `동시 포착` 항목은 `주변 카메라` 로 대체 |
| 이동 경로 | 분할 수만큼 1세트(2/3/4개), 현재 구간 강조, 세트 끝나면 다음 세트로 자동 전환, 4개 미만이면 빈 칸 | v0.8 |
| 주변 카메라 | 좌측 상단 메인 영상 유지 + 같은 시간대·인접 구역 카메라를 **인물 포착 여부와 무관하게**(포착 카메라 우선) 채움, 메인 재생 시점 동기화 | v0.8 |
| 분할 배치 | 2분할 좌우 · 3분할 3열 · 4분할 2×2 / 영역 전환(좁은 칸)은 분할 수만큼 위아래 | 사양 이미지는 4분할만 있음 — 2·3분할 배치는 임의 |
| 칸 머리말 | 카메라명 · 촬영 일시 · 북마크 · 영상 비율 · 영상 확대(⋯ 메뉴 대체) | v0.8 PTS_DTL_004_1 캡처 — GUI 시안(⋯)과 **충돌**, 사양 따름 |
| 표기 | 사양 드롭다운 `이동경로` → `이동 경로` 로 띄어 씀 | 표기 통일 |

## 2026-09-14 · 상세 사양 v0.8 — 청크 C : 타임라인

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 하단 편집 버튼 | ✎ `경로 편집` · ✂ **`영상 클립 편집`**(필름 아이콘 → 가위) | v0.8 3-5 · PTS_DTL_002_1 |
| 클립 편집 | 최소 간격 **5초**(1분 → 5초), 핸들 교차 불가. 변경이 있을 때만 `저장` 활성 → “저장되었습니다.” 토스트 후 종료 / `취소`·ESC : 변경 없으면 바로 종료, 있으면 “편집 취소” alert(확인 시 원래 구간 복원). 경로 비교에서도 모든 인물 트랙에 핸들 | v0.8 PTS_DTL_002_1 2·3 |
| 클립 카드 | 대표 썸네일 · 순번 · **위치명(하단)** · 동일 위치 카메라 수. 좁은 구간(64px 미만)은 위치명·카메라 수 생략, 표기가 불가능한 22px 미만만 썸네일까지 생략 | v0.8 3-4. 사양은 “협소 시 내부 이미지 생략” — 썸네일 유지 구두 지시와 절충해 기준을 22px 로 |
| 구간 호버 미리보기 | 탐지 구간에 마우스를 올리면 그 위치의 영상 썸네일 + `HH : MM : SS` 팝오버(인물 정보 없음). 커서 호버 미리보기 유지 | v0.8 3-3 |
| 인물 칩 | **이미지 호버 = 상세 툴팁**(사진 · 이름 · 탐지 일시 · 인물 등록 ›), **이름 영역 호버 = 삭제 ✕**. 툴팁이 ✕ 를 가리던 문제 근본 해결 | v0.8 3-1 · 경로 비교 2-1 |

## 2026-09-14 · 상세 사양 v0.8 — 청크 D : 경로 비교 · 멀티 뷰 보완

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 경로 비교 진입 | **보던 상세 탭 안에서 전환**(새 탭 생성 · `경로 비교 > …` 제목 삭제, 탭 제목 유지). 한 명만 남으면 같은 탭이 원래 상세로 복귀 | v0.8 PTS_DTL_CMP_000_1 (탭 캡처 `인물 A 1F 메인 복도` 유지) |
| 비교 칸 머리말 | 인물명 · 위치명 · **현재 재생 시각**, 모든 인원 북마크 · 비율 · 확대(3·4명의 ⋯ 대체). 현재 시점에 그 인물 영상이 없으면 위치명·일시 `-` 와 검은 화면 | v0.8 1-1 · 3/5 · 4/5 캡처 |
| 멀티 뷰 칸 비율 | 기본 **원본 비율**(레터박스) — 자르기로 들어가던 문제 수정 | 2026-09-14 구두 |
| 멀티 뷰 휠 확대 | 2·3·4분할 모두 칸 위 휠로 그 칸만 확대/축소(1.0~6x), 드래그 이동 · 칸 좌측 하단 전체 썸네일. 칸별 비율·확대 상태는 다시 그려도 유지. 확대 중 드래그는 구간 이동으로 치지 않음 | 2026-09-14 구두 |

## 2026-09-14 · 상세 사양 v0.8 — 청크 E : 맵 뷰어

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 층 이동 드롭다운 | `A동 > 1층 (탐지 수)` 형식, **인물이 검출된 층만**(+ 외부 구역 `외부 (N)`). 고르면 그 층으로 전환(자동 추종 중지), 긴 이름은 말줄임 + 툴팁 | v0.8 4-1. 기존 `A동 (N건)` 대체 |
| 타 구역 이동 | 드롭다운 옆 `←` `→` 버튼(호버 툴팁 `이전/다음 구역(구역명)`, 없으면 비활성). 누르면 그 구역 지도로 즉시 전환 · 첫 탐지 지점 층으로. 경로 비교는 버튼 → 인물별 `인물 A 다음 구역 (외부)` 목록 | v0.8 4-1 · 경로 비교 3-1. 지도 위 떠 있던 `구역 이동` 칩은 **삭제** |
| 다층 뷰 안내 | “마우스를 드래그하여 시점을 변경할 수 있습니다.” **5초**, 다층 뷰를 다시 켜면 시점 초기화 | v0.8 MAP_000_1 1. 사양 “실제 포착된 층만” 은 **구두(4개 층 모두 · 미검출 층 50%) 유지** |
| 외곽 클러스터 배지 | 묶인 지점 **순번 표기** — 연속 `6~9`, 비연속 `10(+2)` | v0.8 MAP_000_1 2. 주변 카메라 회색 클러스터(100+)는 외곽 카메라 데이터가 없어 미구현 |
| 주변 카메라 아이콘 | 현재 재생 카메라(파랑) · 대상 포착 카메라 · 대상 미포착 카메라(흐리게) 3유형 + 툴팁 | v0.8 MAP_000_2 [카메라 아이콘 유형] |

## 2026-09-14 · 상세 사양 v0.8 — 청크 F : 영역 검색 · 가상펜스 검색

| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 영역 검색 모드 | 진입 시 영상 자동 정지 · 분석 도구 비활성, 드래그로 사각형 → `재실행` · `검색`. 영역 안 객체 1개면 바운딩 박스 + `취소` · `확인`. 버튼 재선택·ESC 로 해제 | v0.8 PTS_DTL_SRC_000_1. 단일 객체 판정은 목업(작은 영역) |
| 영역 검색 팝업 | 제목 `영역 검색` · “영역에서 자동 검출된 인물을 검색할 수 있습니다.” · 프레임에 검출 박스 · 전체/얼굴/외형 · 카드(썸네일 · 포착 시간) · **처음엔 미선택, 선택해야 `검색` 활성** · 결과 없음 “검출된 인물이 없습니다.” · `검색` → **영상 선택 팝업** | v0.8 PTS_DTL_SRC_000_2 |
| 가상펜스 드로잉 | 시작점·종료점 클릭(① ② 핸들), `방향 지정` → 선 중앙에 수직 화살표, 화살표 클릭 시 반대 방향 · `재실행` · `검색` | v0.8 PTS_DTL_SRC_0001_1 |
| 가상펜스 검색 팝업 | 기준선 통과 이벤트 영상 카드(시간순, 1개 선택) · 결과 없음 “해당 기준선을 통과한 이벤트 영상이 없습니다.” · `취소` · `다음`(1개 선택 시 활성) | v0.8 PTS_DTL_SRC_0001_2 1 |
| 인물 선택 팝업 | “영상에서 검출된 인물을 검색할 수 있습니다.” · 전체/얼굴/외형 · `이전`(가상펜스 팝업으로, 선택 유지) · `검색` → **검색 결과 탭의 인물 > 이미지 검색**으로 이동해 같은 과정 수행 | v0.8 PTS_DTL_SRC_0001_2 2 |
| 영상 전체보기 팝업 | 제목 `영상 조회` → **`영상 전체보기`**, 머리말 북마크 · 영상 비율 · 영상 확대(전체보기 버튼 없음), 재생 제어만(검색·분석 도구 없음) | v0.8 PTS_DTL_001_3 |

## 2026-09-14 영상·타임라인·지도 재생 동기화 (구두 지시 "모두 싱크가 맞아야 함")
- 실제 구간(수십 분~수 시간)을 그대로 재생하면 커서가 거의 안 움직여 동작을 알아볼 수 없다(구두 : "이렇게 동작하는구나를 알 수 있는 수준"). 목업에서는 **구간 하나를 구간 폭에 비례한 5~15초**에 재생한다.
  - 재생 : 커서가 구간을 5~15초(×배속)에 지나가고, 영상은 1배속으로 앞부분(진행률 × 재생 초)을 쓴다. 지도 현재 위치는 커서 시각을 따라간다.
  - 구간 끝 → 사이 빈 시간을 건너뛰고 다음 구간 시작으로. 메인 영상 · 카메라명 · 시각 · PIP 가 그 구간 카메라로 바뀐다. 마지막 구간이 끝나면 정지.
  - 커서 끌기 · 지도 지점 클릭 · 10초 이동도 같은 규칙(10초 이동 = 영상 10초 = 구간 × 10/재생 초).
  - 경로 비교 : 칸마다 그 인물의 구간 영상, 구간 밖이면 정지(머리말 `-`). 멀티 뷰(이동 경로) : 현재 구간 칸만 재생, 지난 칸은 끝 프레임 · 다음 칸은 첫 프레임.
  - 역방향 배속은 영상을 멈추고 커서를 따라 프레임을 되감는다.
- 확인 필요 : 실제 제품은 녹화 시각 그대로 재생(1초=1초)이 기본일 것 — 사양서에 재생 속도 · 구간 사이 건너뛰기 규칙 명시 필요.

## 2026-09-14 다채널 동시 포착 PIP 는 1칸에만 (구두 : "프로토타입이니 1개 영상에서만")
- 멀티 뷰(이동 경로) : 동시 포착 PIP 는 **지금 재생 중인 칸**에만, 나머지 칸은 단일 영상. (주변 카메라 유형은 원래 PIP 없음)
- 경로 비교 : PIP 는 **첫 칸(기준 인물)** 에만, 나머지 칸은 단일 영상.
- 사양 v0.8 은 1/2/3/4 멀티뷰 모든 칸에 PIP — 목업 부하·가독성 때문에 축소. 실제 제품 적용 범위 확인 필요.

## 2026-09-14 경로 비교 보완 (구두)
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 맵 뷰 현재 위치 | 인물마다 **각자의 현재 위치 마커**(인물 색 테두리)를 커서 시각에 맞춰 움직인다. 칩으로 끈 인물은 흐리게. 외부 구역 지도도 인물 수만큼 경로 · 지점 | 사양 PTS_DTL_CMP_000_2 3 "모든 비교 대상의 위치와 이동 동선". 예전엔 모든 지점을 한 줄로 섞어 마커 하나만 오갔음 |
| 영상 없는 구간 칸 | 커서 시각에 그 인물 구간이 없으면 영상 레이어(미리 보이던 썸네일)까지 숨겨 **검은 화면** + 머리말 `-` | 사양 1-1) |
| 경로 비교 멀티 뷰 | 비교 툴바 [단일] [멀티 뷰 ▾] 활성(히트맵 · 인접 카메라 · 영역 검색은 계속 흐림). 멀티 뷰면 비교 칸 대신 **기준 인물(첫 트랙)** 의 멀티 뷰(이동 경로 / 주변 카메라 × 2·3·4분할), 재생 · 커서와 동기화 | 구두 "인물 비교 시에도 카메라 멀티 뷰가 작동하는 사양". 사양서 비교 1-3) 에는 멀티 뷰 언급 없음 — **어느 인물 기준인지 · 인물별 멀티 뷰 여부 확인 필요** |

## 2026-09-14 맵 뷰 층 이동 드롭다운 재정비 (구두 : "사양 기준으로 다시")
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 드롭다운 위치 | 동 선택 버튼 **바로 아래** 150px 목록. 예전엔 기준 요소가 지도 영역이라 화면 하단에 넓게 열렸음 | 시안 4876:149741 |
| 항목 표기 | `A동 > 1층` + 흐린 `(탐지 수)`, 선택 항목 ✓ + 밝은 바탕, 항목 높이 28 · 간격 8, 열리면 ▲ | 사양 PTS_DTL_001_5 4-1 |
| 선택 동작 | 그 층(구역) 지도로 즉시 전환 · 커서 자동 추종 중지. **다층 뷰에서 층을 고르면 다층 뷰를 끄고 그 층 단독 도면** | 사양 "항목 선택 시 해당 층으로 전환". 다층 뷰 중 선택 동작은 사양에 없음 — 확인 필요 |
| 구역 이동 툴팁 | `이전/다음 구역 (구역명)` 파란 말풍선 | 시안 4876:149762 · 149764 |

## 2026-09-14 GUI 시안 갱신 반영 (검색 상세 GUI_260914 · 5254:55657) — 1A 영상 제어 막대 · 팝오버
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 제어 막대 아이콘 | 영역 · 선 검색 / 이전 · 되감기 · 10초 뒤 · 재생 · 10초 앞 · 빨리감기 · 다음 / 히트맵 · 인접 카메라 · 단일 · 멀티 · 전체화면 — **GUI 에서 SVG 추출**(24 박스 · 원본 위치 유지, assets/icons/g-*.svg) | 5254:69513 |
| 멀티 뷰 버튼 | 글자 · ▾ 없이 **아이콘만**. 누르면 멀티 뷰 전환 + 팝오버(`재생영상 이동 경로 ›` · `화면 분할 4분할 ›`), 행을 누르면 목록으로 들어가 `‹ 재생 영상` 머리로 복귀. 상세 · 경로 비교 공통 | 5254:69796. 시안 두 번째 행 이름이 `재생영상` 으로 중복 → **`화면 분할`로 수정** |
| 영상 확대 배율 | 1.0 · 2x · **3x** · 4x (기존 6x 삭제) | 5254:69626 |
| 비율 · 확대 팝오버 | 140 · 행 28 · 12R, 선택 파랑 · 호버 밝은 바탕 | 5254:69626 |
| 설정 팝오버(재생속도 · 반복 재생 · 음량) | **미반영** — 시안에 팝오버만 있고 여는 버튼이 제어 막대에 없음. 설정 버튼은 2026-09-11 구두로 삭제한 상태 | 확인 필요 : 설정 버튼 위치 · 부활 여부 |

## 2026-09-14 GUI 260914 반영 — 1B 레이아웃
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 상세 화면 LNB | 상세 · 경로 비교 탭에서 **세로 메뉴 숨김**(검색 화면은 유지). 예전 코드에 `사양 : 상세화면 진입 시 LNB 미노출` 주석만 있고 규칙이 없었음 | 5254:69505 |
| 영역 비율 | 영상 1280 · 간격 8 · 우측 패널 616(맵 뷰어 592, 좌우 12 · 상하 16) | 5254:69510 · 69570 |
| 탭 | 200 · `인물명`(진하게) + `위치명`(흐리게) | 5254:69508 |
| 영상 머리말 시각 | 구간(`시작 ~ 끝`) → **현재 재생 시각 하나**, 커서 · 재생에 맞춰 갱신 | 5254:69505 `1F 메인 복도 2026-06-29 00:52:03`. 사양 v0.8 2-1) 은 구간 표기 — 확인 필요 |
| 맵 머리말 | 동 선택 140 · 이전/다음 구역 34×34(아이콘 추출) · 단일/듀얼 34 세그먼트 · 바꾸기 34×34. 구역 툴팁은 **회색 말풍선**(사양서 이미지의 파란 말풍선 대체) | 5254:69573 · 69586 |
| 헤더 AI 버튼 | **숨김 유지** — 2026-08-31 "AI 기능 제외" 구두 지시 | 시안에는 있음 · 재개 시 S.aiVer='on' |
| 맵 영역 레터박스 · 하단 범례 | 다음 청크(맵 뷰)에서 반영 | 5254:69838 |

## 2026-09-14 GUI 260914 반영 — 1C 타임라인
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 눈금 | 1280 폭에서 **1시간 간격**(약 50px), 자정은 `24:00` 표기 · 날짜는 시작과 날이 바뀌는 지점 | 5254:69554 |
| 하단 조작 아이콘 | 경로 편집(write) · 영상 클립 편집(edit-movie) · 축소/확대 — **GUI 에서 추출**, 24 · 간격 16, 슬라이더 96 | 5254:69554 Timeline Control |
| 인물 칩 호버 카드 | 254 · 여백 11 · 간격 12 · 모서리 6 · 그림자, 원형 사진 80 · 이름 12M · 기간 10R · `인물등록 ›` 12M | 5254:69555 |

## 2026-09-14 GUI 260914 반영 — 2 영상 머리말 · 멀티 뷰 칸 · 영상 조회 팝업
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 영상 머리말 아이콘 | 북마크 · 비율설정 · 확대 — **GUI 에서 추출**, 24 · 간격 16 | 5254:69512 action |
| 재생 중 버튼 | 일시정지 아이콘 GUI 추출본으로 교체 | 5254:69754 ic_일시정지 |
| 멀티 뷰 칸 머리말 | 위 80px 어둡게 · `위치명(12M) 현재 재생 시각(12R)` · 우상단 **⋯ 하나**(북마크 · 영상 비율 · 전체 보기 메뉴). 시각은 커서 · 재생에 맞춰 갱신 | 5254:69776. 사양 v0.8 의 칸별 3아이콘 대체 |
| 전체보기 팝업 | 제목 **`영상 조회`**(14M, 앞 아이콘 없음) · 닫기 20 추출 · 머리말은 북마크 · 비율만(확대 없음) · 구간 표기 유지 | 5254:69729 |

## 2026-09-14 GUI 260914 반영 — 3 맵 뷰 (기본 · 도구 · 이동경로)
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 도면 표시 | 어두운 바탕 + 도면을 **원본 비율로 가운데**(흰 판, 레터박스). 경로 데이터 x 는 예전 cover 화면(592×874) 기준이라 그릴 때 도면 기준으로 변환(flatU), 현재 위치 마커는 화면 좌표로 계산 | 5254:69839 · 사양 4) 원본 비율 유지 + 레터박스 |
| 층 배지 | 도면 판 왼쪽 위 모서리 | 5254:69839 |
| 맵 제어 도구 | 우상단 10 · 폭 34 세로 — [다층 뷰] 8 [주변 카메라 · 이동 경로] 8 [+ −], 아이콘 **GUI 추출**, 다층 뷰 켜짐은 파란 채움 | 5254:69839 Map Control |
| 하단 범례 | 상세 화면에서 숨김 (시안에 없음) | 5254:69838 |
| 경로 지점 | 핀 16(흰 테) · 번호 10M, 이름표 반투명 검정 12M 핀 위 | 5254:69839 path marker |
| 이동경로 리스트 | 인물 칸 160 · 패널 178, 동선 버튼 왼쪽에 붙임(버튼을 덮던 문제 — 고정 위치 패널이 남은 화면 폭만큼 늘어나던 원인) | 5254:69853 |

## 2026-09-14 GUI 260914 반영 — 4 외곽 지도 · 듀얼 지도 · 카메라 표시
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 카메라 위치 | 부채꼴 아이콘 46 — 파랑(대상 포착) · 회색(미포착) · 빨강(현재 재생), **GUI 에서 추출**(assets/img/cone-*.svg). 도면 좌표 변환 적용 | 5254:69844 |
| 외곽 지점 | 핀 16 + 위 이름표(반투명 검정 12M, 앞의 `외부` 생략), 마지막 지점은 파란 이름표 + 옅은 테 | 5254:69842 · 69843 |
| 외곽 클러스터 | 가까운 경로 지점은 파란 원 40(`5~6`), 묶임이 있는 배율에선 낱개도 28 원 · 이름표 없음. 경로와 무관한 주변 카메라 무리는 **회색 원 40(40 · 50 · 30 · 100+)**, 확대하면 숨김 | 5254:69841. 회색 무리 개수 · 위치는 목업 고정값 — 실제 데이터 기준 확인 필요 |
| 듀얼 지도 | 칸마다 왼쪽 위 동 선택 140 + 이전/다음 34, 오른쪽 위 도구 줄(다층 · 카메라 · 동선 · + −). 아래 칸 이전/다음은 위 칸과 겹치지 않는 구역 사이 이동, 도구는 위 칸과 같은 동작. 층 도면 칸도 레터박스 | 5254:69841 |

## 2026-09-14 GUI 260914 반영 — 5 영역 검색 · 가상펜스 검색
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 영역 드로잉 | 노란 점선 2 + 노란 10% 채움, 모서리 빨간 점 12. 아래 12px 가운데 `↻(28)` · `검색(64×28)` 어두운 버튼(↻ 아이콘 GUI 추출) | 5254:70147 |
| 단일 객체 검출 | 파란 1px 박스 + 오른쪽 아래 8px `취소 \| 확인` 한 덩어리(확인 파랑) | 5255:99021 |
| 선(가상펜스) 드로잉 | 초록 점선 · 빨간 끝점 12 · 가운데 **방향 원 36**(누르면 반대 방향, 이미지 GUI 추출). `방향 지정` 버튼 **삭제**, 끝점 아래 오른쪽 정렬 `↻ · 검색` | 5254:70185. 사양 v0.8 의 `방향 지정` 버튼 대체 |
| 영역 검색 팝업 | 폭 672 · 프레임 640×360 · 알약 칩 28 · 카드 4열 186(얼굴 파랑 / 외형 주황 배지 + 시각) · 결과 없음 `추출된 인물이 없습니다.` | 5254:70426. 문구 `검출된` → 시안의 `추출된` |
| 가상펜스 검색 팝업 | 폭 998 · 툴바(총 N건 · 썸네일 크기 슬라이더(열 수) · 유사도순 · 썸네일/원본) · 카드 5열(유사도 배지 · 위치 14M · 일시 12R) · 취소/다음 | 5254:70347 |
| 가상펜스 → 인물 선택 | `다음` 뒤 **같은 팝업 안에서** 인물 선택(프레임 · 칩 · 카드 · 이전/검색). 제목은 `가상펜스 검색` 유지, 안내 `선택한 영상에서 자동 추출된 인물을 검색할 수 있습니다.` | 5254:70347 오른쪽 — 시안 제목이 `영역 검색`으로 복사돼 있어 흐름에 맞게 수정 |

## 2026-09-14 GUI 260914 반영 — 6 히트맵 · 인접 카메라
| 항목 | 구현 | 근거·비고 |
|---|---|---|
| 인접 카메라 핀 | 파란 원(여백 4 · 그림자) + 박스카메라 20 — **GUI 추출** | 5254:70253 |
| 핀 호버 미리보기 | 핀 오른쪽 아래 160×90 썸네일 · 아래 20px 어둡게 · 카메라명 10M | 5254:70256 |
| 카메라 선택 시 | 머리말 `← 이전 영상` · 카메라명 · 구간(기존 구현 유지) | 5254:70301. 시안 `이전영상` → 띄어쓰기 `이전 영상` |
| 히트맵 | 시안의 히트맵은 영상 이미지에 합성돼 있어 **에셋 추출 불가** — 생성식 오버레이를 동선 띠 모양 · 흐림으로 근사 | 5254:70213 · 실제 제품은 분석 결과 이미지 |
| (버그) 인접 카메라 핀 | 상세(dt-b) 오버레이 규칙이 핀까지 클릭 불가로 막아 호버 미리보기 · 카메라 전환이 안 되던 문제 수정 | 검증 중 발견 |

## GUI 260914 반영 7 — 경로 편집 · 영상 클립 편집 (5254:70521 · 70492 · 70671 · 70699)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 경로 편집 · 영상 선택 팝업 | 검색 흐름의 영상 선택과 분리해 시안 구조로 새로 구성. 1282 폭 · 왼쪽 기준 인물 사진 200(ⓘ) + 이름 입력 · 오른쪽 `총 N건` · 크기 슬라이더 · 정렬 · 썸네일/원본 전환 · 카드 5열(누르면 선택, 파란 2px) · 아래 시간순 띠(번호 배지 · ⊗ 빼기 · 위치 · 시각) · 확대 슬라이더 · 취소/완료 | 사양서의 경로 편집 영상 선택을 이 구조(날짜 묶음·체크박스 없음, 카드 클릭 선택, 하단 시간순 띠)로 교체 |
| 띠 아래 시각 | 시안은 `00:20:00`(구간 길이로 보임). 목업은 포착 **시각(시:분:초)** 표시 — 순서를 읽기 쉬움 | 길이/시각 중 무엇을 보일지 확정 필요 |
| 완료 후 구간 길이 | 고른 영상마다 20분 구간(다음 구간 시작 전까지)으로 타임라인을 다시 구성 | 실제 구간 산출 규칙 정의 필요 |
| 인물 선택 팝업 | 400 폭 · 행 58 · 라디오 · 이름 · 사진 48 · 취소/선택, 닫기 아이콘 교체. 인물이 2명 이상일 때만 뜬다 | 변경 없음 |
| 영상 클립 편집 | 구간 테두리를 인물 색 2px로, 양 끝 안쪽 흰 손잡이로 변경(주황 손잡이 제거). 휴지통은 마우스를 올렸을 때만 | 시안에 삭제 버튼이 없음 — 편집 중 구간 삭제 허용 여부 확정 필요 |
| 토스트 | 연녹색 바탕 · 녹색 체크 · 닫기 ✕ 로 전체 토스트 통일, 저장 시 `저장되었습니다.` | 오류/안내 토스트도 같은 녹색을 쓸지 확정 필요 |
| 연필/필름 버튼 기능 | 시안은 클립 편집 중 연필이 켜져 있으나, 9/11 구두 지시대로 **연필 = 경로 편집, 필름 = 영상 클립 편집** 유지 | 시안 아이콘 활성 상태를 구두 지시에 맞게 수정 필요 |

## GUI 260914 반영 8 — 지도 위치 변경(맵 뷰어 듀얼) · 영상 위치 전환 (5254:70736 · 69917)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 맵이 넓은 왼쪽일 때 | 맵 상자에 안쪽 여백 12/16 · 배경, 두 칸은 좌우로 · 칸마다 층 선택 · 이동 · 도구 | 변경 없음 |
| 영상이 오른쪽 칸일 때 | 616 폭 여백 없이 꽉 채움 · 머리말 앞에 `■ 인물 A │` 칩 | 변경 없음 |
| 분할(2·3·4) 칸 머리말 | 시안대로 인물 칩 · 위치 · 시각 + 북마크/비율/화면확대 아이콘 24. 넓은 영역(기본 배치)에서는 기존대로 ⋯ 하나 | 배치에 따라 머리말 아이콘이 달라짐을 사양에 명시 |
| 분할 칸 번호 배지 | 오른쪽 칸에서는 숨김(시안에 없음) | 번호 배지 노출 기준 확정 필요 |
| 3·4분할 영상 | 칸을 꽉 채움(잘림 허용), 듀얼은 16:9 전체 보기 | 변경 없음 |
| 재생 조작 바 | 시안 오른쪽 칸에는 재생 조작 바가 없지만, 재생을 할 수 없게 되므로 **아래에 유지** | 시안에 조작 바 위치 추가 필요 |
| (버그 수정) 분할 → 단일 전환 | 분할 뷰에서 단일로 돌아오면 영상이 사라지고 재생 조작 바가 위로 올라붙던 문제 수정(배치와 무관하게 발생하던 기존 버그) | 변경 없음 |

## GUI 260914 반영 9 — 경로 비교 2·3·4명 · 인물 호버 및 비활성 (5254:70815 · 70836 · 70863 · 70892)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 영상 영역 위치 | 경로 비교 영상 영역의 왼쪽 여백 20 제거 — 상세와 같은 자리·폭(1280) | 변경 없음 |
| 칸 머리말 아이콘 | 2명은 북마크·비율·확대 아이콘, 3·4명은 ⋯ 하나(누르면 북마크·영상 비율·전체 보기) | 인원수에 따라 머리말 아이콘이 달라짐을 사양에 명시 |
| 인물 칩 빼기 | 타임라인 인물 칩에 마우스를 올리면 썸네일 우상단에 ⊗ 16 표시(기준 인물 제외) | 변경 없음 |
| 인물 비활성 | 끈 인물은 영상 칸을 비우고 머리말을 `인물 C -` 로, 타임라인 행은 흐리게 | 변경 없음 |
| 영상 없는 구간 | 시안은 `-` 표시와 함께 영상이 보이지만, 9/14 구두 지시대로 **영상 없는 구간은 칸을 비움** 유지 | 시안 수정 필요 |

## GUI 260914 반영 10 — 이동경로 리스트 팝업 · 경로 비교 추가 팝업 · 지도 다층 뷰 (5254:71091 · 71360)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 이동경로 리스트 | 닫기 16 아이콘, 위치명 12R, 시각·카메라 10R, 층·구역 이동 띠를 회색 바탕으로(점선 제거) | 시안 문구 `층이동` → **`층 이동`** 으로 띄어 씀(맞춤법) |
| 경로 비교 추가 팝업 | 440 폭, 날짜 묶음 사이 구분선, 행 = 사진 48 · 이름 14M + 위치 배지(출현 지점 수) · 일시 12R, 체크박스 오른쪽 18. 기준 인물은 회색으로 잠김 | 부제 `경로를 함께 비교할 인물을 추가해 주세요. (최대 3명)` 로 변경. 배지 숫자 = 출현 지점 수로 해석 — 확인 필요 |
| 다층 뷰 호버 툴팁 | 문구 `마우스를 드래그하여 360° 로 회전할 수 있습니다.`, **3초** 후 사라짐, 시안 색·여백 | 사양 v0.8(5초)과 다름 — 3초로 수정 필요 |
| 다층 뷰 3D 조작 버튼 | 시안 ic_3D 아이콘을 추출해 48 원형 버튼으로 교체(우하단 19/24) | 변경 없음 |
| 3가지 안내 방식 | 시안은 `GUI 제안`(호버 툴팁 / 중앙 하단 상시 / 3D 버튼) 3안 — 기존처럼 설정으로 바꿔 볼 수 있게 유지, 기본은 1안 | 최종 1안 확정 필요 |

## 검색 홈 탭 · 패널 (2026-09-15 구두 · 시안 4965:30876 · 4965:31318 · 4982:67933)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 패널 구성 | 검색 패널 · 결과 · 원본 영상을 **한 장의 카드**로 붙임(사이 8px 틈과 테두리 제거 → 1px 구분선, 위아래 8 들여쓰기). GNB(LNB)는 기존처럼 별도 카드 | 변경 없음 |
| 패널 높이 | 결과 · 원본 영상 모두 탭 줄(28) 바로 아래에서 시작(원본 영상이 맨 위까지 올라가 있던 것 수정) | 변경 없음 |
| 모서리 | 카드 바깥 모서리만 6 라운드, 패널끼리 맞닿는 안쪽은 직각 | 변경 없음 |
| 탭 | 140×28 · 12M 흐린 글자 · 탭마다 오른쪽 1×16 구분선, 탭 줄이 원본 영상 위까지 이어져 히스토리가 오른쪽 끝 | 변경 없음 |
| 패널 머리(검색 탭) | 탭 줄과 같은 28 높이 · 12R, 접기 아이콘은 시안 ic_패널접기 추출로 교체. 라벨은 9/10 검색 화면 사양대로 `검색 결과` 유지 | 시안 라벨 `검색` → `검색 결과` 로 수정 필요 |
| 유지한 것 | 화면 바탕색은 상세 시안과 같은 level-0(#0d0e12) 유지(검색 홈 시안은 #131418), `＋` 새 탭 버튼 · 탭 닫기 ✕(마우스 올릴 때) 유지 | 시안에 새 탭 · 닫기 표시 추가 필요 |

## 상세 화면 탭 (2026-09-15 구두 · 시안 5254:70739)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| `검색 결과` 탭 | 12M · secondary 색(흐린 tertiary 였던 것 수정) | 변경 없음 |
| 활성 탭 | level-2 바탕 · 위 모서리 6 · 인물명 12R primary + 위치 12R tertiary(간격 4) | 변경 없음 |
| 비활성 탭 | 왼쪽 8 · 간격 10 · 12R(위치명 500 → 400), 구분선은 탭 오른쪽 끝 · 활성 탭 바로 앞과 마지막 탭은 없음 | 변경 없음 |
| 닫기 ✕ | 시안에는 없음 → 탭을 닫을 수 있어야 하므로 **마우스를 올렸을 때만** 표시 | 시안에 닫기 표시 방식 추가 필요 |

## 경로 비교 칸 동시 포착 위치 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 2·3명 칸 동시 포착 | 영상 상자(16:9 레터박스) 안 하단 → **칸 맨 아래**로 옮김. 영상 아래를 가리지 않고 영상 밑 빈 공간에 놓인다(시안 70815 · 70836 과 같은 배치). 4명은 영상이 칸을 채워 위치 변화 없음 | 변경 없음 |
| 멀티 뷰 3분할 | 이미 칸 맨 아래에 놓여 있어 변경 없음 | 변경 없음 |

## 영상 휠 확대 · 탭과 영상 간격 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 탭 ↔ 영상 간격 | 상세 영상 영역이 탭 바로 아래(y72)에서 시작하고, 영상 · 재생 바 · 타임라인 사이 10px 틈 제거(경로 비교 동일). 줄어든 틈만큼 영상이 늘어남 (시안 5254:69505) | 변경 없음 |
| 휠 확대 범위 | 멀티 뷰 칸에서만 되던 휠 확대/축소를 **단일(1분할) 영상 · 경로 비교 2·3·4명 칸**까지 같은 동작으로. 재생 바 · 머리말 · 주변 카메라 · 축소 지도 위에서는 확대하지 않음, 영역/선 검색 그리기 중에는 동작 안 함 | 사양 `휠 = 확대 모드에서만` 문구를 `영상 위 휠 = 확대/축소(모든 분할)` 로 수정 필요 |

## 영상 클립 편집 손잡이 (2026-09-15 구두 · 첨부 시안)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 편집 중 구간 모양 | 2px 테두리 + 영상 위 흰 막대 → **인물 색 두꺼운 틀**(좌우 12 · 위아래 6, 모서리 4) 안에 영상 띠, 위 8px 색 막대는 틀에 합쳐 숨김 | 변경 없음 |
| 손잡이 | 좌우 틀(12) 가운데 흰 막대 3×24 · 둥근 끝, 영상과 겹치지 않음. 좁은 구간에서도 막대 표시 | 변경 없음 |
| 위아래 틀 두께 | 시안은 약 7px(행 높이가 더 큼) — 타임라인 행 높이(65)를 유지하려고 6px 로 맞춤 | 행 높이 확정 필요 |

## 탭 이름 중복 · ＋ 버튼 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 탭 이름 | 대상명이 없어 위치명이 이름 자리에 들어오면 `외부 CCTV 외부 CCTV` 처럼 두 번 보이던 것 → 한 번만 표시 | 변경 없음 |
| 탭 줄 `＋` | 시안에 없고 탭은 상세 진입 시 생기므로 **숨김**(새 검색 탭 기능 코드는 남겨 둠) | 사양서의 `새 탭 추가` 항목 삭제 필요 |
| 클립 편집 좁은 구간 | 폭 64 미만 구간은 좌우 틀 7 · 막대 2×20 로 줄여 영상이 보이게 | 변경 없음 |

## 맵 뷰 머리 버튼 · 주변 카메라 · 초기화 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 지도 두 칸 머리 버튼 | 칸 위에 올라간 층 선택 옆 ← → 가 투명 바탕이라 밝은 지도에서 보이지 않던 것 → 어두운 바탕 · 테두리, 비활성은 아이콘만 흐리게 | 변경 없음 |
| 검색 홈 초기화 | 검색 조건(유사도 · 기간 · 색상 · 카메라)과 함께 **입력한 검색어도 비움** | 사양서 초기화 범위에 `검색어` 추가 필요 |
| 외부 지도 주변 카메라 | `주변 카메라` 를 켜도 외부 지도에는 아무것도 안 나오던 것(도면 좌표 원뿔만 있었음) → 출현 지점마다 그 카메라를 지점 쪽으로 향하게 표시 + 미포착 카메라 3대. 마우스 → 미리보기, 누르면 영상 조회. 지도 두 칸의 외부 칸에도 동일, 두 번째 칸 도면에도 원뿔 표시(켜도 첫 칸에만 나오던 것) | 외부 카메라 실제 위치 데이터 필요(목업은 지점 옆 임의 배치) |

## 검색 홈 줄 맞춤 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 검색 유형 칩 줄 ↔ 결과 머리 | 칩 줄 중심(108)과 `총 N 건 · 결과 더보기` 줄 중심(97)이 11px 어긋나 있던 것 → 둘 다 y100 (시안 4965:30876 과 같은 높이) | 변경 없음 |
| `7개 선택 ✕ ⓘ` 칩 | 글자 · ✕ · ⓘ 세로 중심을 한 줄로 맞추고 간격 4 (위쪽 규칙에 눌려 정렬이 안 먹던 것 수정) | 변경 없음 |
| 히스토리 버튼 | 패널 끝에서 4px 로 붙어 있던 것 → 아래 원본 영상 카드 오른쪽 끝(안쪽 15)에 맞춤 | 변경 없음 |

## 검색 결과 카드 더보기 — 북마크 중복 제거 (2026-09-15 구두 · 첨부 시안)

카드 하단에 북마크 버튼이 있는데 `⋯` 를 누르면 북마크가 또 나오던 것을 정리했다.
(하단 북마크 `.cbk` 는 9/9 와이어프레임 반영 `7ae4387` 때 들어왔고, 그 전부터 있던 `⋯` 의 북마크 항목이 남아 중복이 됐다)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 카드 북마크 | **하단 버튼(16) 하나만** 유지 — 시안이 썸네일 아래 북마크·`⋯` 를 나란히 둔다 | 변경 없음 |
| `⋯` 버튼 노출 | **지능형 알고리즘 검색 결과에만** 붙인다. 그 밖의 검색 유형(텍스트·차량번호·인물·이미지·AI)은 **북마크 아이콘만** | 사양서에 `⋯` 노출 조건 = 지능형 알고리즘 결과 임을 명시 필요 |
| `⋯` 더보기 항목 | 북마크를 빼고 **`인물 검색 · 오탐지 신고`** 2종 | 사양서 §"검색 결과 카드 더보기 = 북마크 / 오탐지 신고" 에서 **북마크 삭제** 필요 |

확인(Chromium) : 텍스트 검색 = 북마크 16×16 만(`⋯` 없음) · 지능형 `⋯` = `인물 검색 · 오탐지 신고` 2개 · 하단 북마크 토글 정상 · 페이지 오류 없음

## 타임라인 인물 칩 — 컬러 테두리 삭제 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 인물 칩 테두리 | 칩(`.tl-obj`)을 두르던 **인물 색 라인을 전부 제거**. 기본 1.5px · B안 1px · 비활성 행의 회색 테두리까지 | 사양·시안의 `인물 색 = 칩 테두리` 표기 삭제 필요 |
| 남는 인물 색 구분 | 이름 앞 **8×8 점**(`.nm i`)과 **클립 막대·썸네일 틀**(`--bc`)이 계속 맡는다 | 변경 없음 |

확인(Chromium) : 상세 1트랙 · 경로 비교 4명 · 인물 비활성 행 모두 테두리 `0px`, 칩 80×65 유지(border-box), 점 색 A #0099ff · B #ff2ec4 · C #ffab00 · D #00d665 그대로, 페이지 오류 없음

## 타임라인 인물 칩 누름 동작 — 활성/비활성 → 상세 정보 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 칩 누름 | 종전 **활성/비활성(Dim) 토글 삭제**. 누르면 그 인물의 **상세 정보 카드**가 뜬다(✕ · 바깥 클릭 · Esc 로 닫힘) | 사양 4759:35111 의 `칩 누름 = 인물 Dim` **삭제** 필요 |
| 상세 정보 내용 | 썸네일 64 · 인물명(색 점) · **유사도** · **출현 구간**(첫~마지막) · **구간 수 · 카메라 수** · 카메라 목록 · `인물등록 ›` | **필드 구성은 임의** — 사양에 대상 상세 항목 정의가 없다. 확인 필요 |
| 칩 호버 | 종전대로 ✕(빼기) 노출. 이미지 호버 툴팁도 유지하되, 상세 정보가 열려 있으면 덮어쓰지 않는다 | 변경 없음 |
| ✕ 누름 | 종전대로 §14 Alert `인물 삭제 — 선택한 인물이 삭제됩니다.` 확인 후 삭제 | 변경 없음 |
| `TL.off` | 비활성 상태값 자체는 영상 칸·지도가 참조하므로 **코드는 남겨 둠**(아카이브). 켜는 경로가 없어져 항상 비어 있다 | 인물 비활성 기능을 완전히 뺄지, 다른 진입점을 둘지 확인 필요 |

확인(Chromium, 경로 비교 4명) : 칩 클릭 → 상세 정보 표시(인물 B · 유사도 98% · 구간 4개 · 카메라 4대) · 바깥 클릭 닫힘 · 호버 시 ✕ opacity 1 · ✕ → Alert → 삭제되어 칩 4개로 · `TL.off` 계속 빈 값(토글 사라짐 확인) · 페이지 오류 없음

## 탭 줄 — 접힌 `검색 결과` 탭 폭 · 선택/비선택 색 대비 (2026-09-15 구두 · 시안 수정분)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 접힌 `검색 결과` 탭 폭 | 내용 폭(`width:auto` · `min-width:98px`) 이라 **98px** 로 옆 인물 탭 140px 과 어긋나 있었다 → **140px 고정**(다른 탭과 동일) | 변경 없음 |
| 선택/비선택 색 대비 | 선택 `#1c1d23`(bg-2) ↔ 비선택 `#0d0e12`(bg-0) 이 15레벨 차이뿐이라 어느 탭이 선택인지 구분되지 않았다 → **비선택을 `#0a0b0e` 로 눌러** 탭 줄 바탕보다 어둡게, 글자도 선택 `#ecedf0` Medium ↔ 비선택 `#8a8c96` 로 벌림 | **색값은 임의** — 수정된 시안의 확정값 확인 필요 |
| 검색 홈 비선택 탭 | 종전 투명이라 바탕에 묻히던 것도 같은 `#0a0b0e` 로, 호버는 `#141519` | 변경 없음 |

확인(Chromium) : 접힘 홈 탭 x = 76 · 216 · 356 (모두 140 간격) · 선택 `rgb(28,29,35)`/글자 `rgb(236,237,240)` ↔ 비선택 `rgb(10,11,14)`/글자 `rgb(138,140,150)`, 상세·홈 동일, 페이지 오류 없음

## 검색 홈 원본 영상 미리보기 — 진행 바 이동 · 재생/일시정지 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 진행 바 조작 | 종전에는 **표시만 하고 조작이 안 됐다**(핸들러 없음) → 눌러/끌어 그 지점으로 이동. 실제 `<video>.currentTime` 을 옮긴다 | 사양 §6-2 에 미리보기 **탐색(seek) 동작** 추가 필요 |
| 재생/일시정지 | 종전에는 `.paused` 클래스(스틸 팬·줌 애니메이션)만 바뀌고 영상은 계속 돌았다 → `<video>.play()/pause()` 를 같이 건다 | 변경 없음 |
| 시각·길이 표기 | 고정 10초 데모 값 → **실제 영상 길이 기준**(`00:48 / 01:00`). 영상이 아직 안 붙었으면 종전 10초 루프로 | 미리보기 길이를 원본 길이로 볼지 확인 필요 |
| 진행 바 히트 영역 | 두께 3px 라 잡기 어려워 위로 **13px 투명 영역**을 얹고 호버 시 5px 로 | 변경 없음 |

확인(Chromium) : 일시정지 → `currentTime` 41.39 로 1.2초간 고정 · 재개 → 42.7 로 진행 · 진행 바 20%→80% 드래그 → `currentTime` 48.2 (= 0.80 × 60초) · 페이지 오류 없음

## 검색 결과 카드 — 한 번 클릭 / 두 번 클릭이 상황 따라 안 먹던 문제 (2026-09-15 구두)

원인 두 가지가 겹쳐 있었다.

1. 한 번 클릭의 220ms 타이머가 끝나면 `selectCard → renderResults` 로 **결과 카드를 전부 다시 그린다.**
   두 번째 클릭이 새로 만들어진 다른 노드에 떨어져 브라우저 `dblclick` 이 아예 발생하지 않았다.
2. 첫 미리보기가 열릴 때 오른쪽 패널이 생기며 결과 영역이 좁아져 **카드가 밀린다.**
   검색 후 첫 더블클릭이 특히 자주 실패한 이유다.

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 판정 방식 | 네이티브 `dblclick` 을 쓰지 않고 **클릭 수를 직접 센다.** 판정이 끝나기 전에는 다시 그리지 않아 카드가 제자리에 남는다 | 변경 없음 |
| 두 번째 클릭 대기 | 220ms → **350ms** (윈도우 기본 더블클릭 간격이 500ms 라 220ms 는 너무 짧았다) | 미리보기가 350ms 뒤에 뜬다 — 체감 지연 허용 여부 확인 필요 |
| 다른 카드로 옮겨 클릭 | 앞 카드의 셈을 버리고 새로 센다(연달아 다른 카드를 눌러도 상세로 새지 않는다) | 변경 없음 |

확인(Chromium, 6가지) : 검색 직후 첫 더블클릭(90ms) → 상세 · 느린 더블클릭(280ms) → 상세 · 한 번 클릭 → 미리보기만 · 다른 카드 연속 클릭 → 상세 안 열림 · 패널 열린 뒤 더블클릭 → 상세 · 네이티브 dblclick → 상세 1회만(팝업 1개) · 페이지 오류 없음

## 미리보기 — 영상이 타임바보다 늦게 시작하고 튀던 문제 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 타임바 선행 | 영상이 붙기 전 **10초짜리 가짜 루프**가 먼저 돌아 바만 앞서 갔다. 영상이 붙는 순간 기준이 10초 → 60초로 바뀌며 값이 튀어 랙처럼 보였다 → 영상이 준비될 때까지 **바를 0% 에 세워 둔다**(시각 `00:00` · 길이 `--:--`) | 변경 없음 |
| 붙는 순간 쓸림 | 첫 프레임은 `transition` 을 껐다가 다음 프레임에 되돌려, 0% 에서 현재 위치로 쓸려 가지 않게 | 변경 없음 |
| 받기 시작 시점 | 카드를 누른 **즉시** 같은 mp4 를 떼어 놓은 `<video>` 로 미리 받는다(`pvWarm`). `<video>` 를 붙이는 MutationObserver 30ms 대기도 `vidScan()` 한 번으로 당김 | 변경 없음 |

확인(Chromium) : 클릭 후 3초간 100ms 간격 표본 — 영상 준비 전 바 값 변화 없음, 준비(약 0.5초) 후 바 % 와 `currentTime/duration` 이 계속 일치(65.2% / 39.15초 ÷ 60초), 페이지 오류 없음

> 남은 지연은 **한 번/두 번 클릭 판정 대기 350ms** 가 대부분이다. 미리보기가 더 빨리 떠야 하면 이 값을 낮추면 되는데, 그만큼 느린 더블클릭이 다시 실패한다.

## GNB 카드 배경을 검색 패널과 같은 톤으로 (2026-09-15 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| GNB(LNB) 배경 | `.shell > .lnb` 가 `--bg-1`(#131418), 검색 패널(초기화·검색 줄이 있는 그 카드)은 `--bg-2`(#1c1d23) 라 나란히 놓인 두 카드의 톤이 달랐다 → **둘 다 `#1c1d23`** | 인수인계 8번 `화면 바탕색 통일 여부` 와 같은 건 — 카드 면은 bg-2, 바탕은 bg-0 로 정리되는지 확인 필요 |

확인(Chromium) : GNB `rgb(28,29,35)` = 검색 패널 `rgb(28,29,35)`, 바탕(셸·body) 은 `rgb(13,14,18)` 그대로

## ~~❓~~ 정렬 줄 아이콘이 시안과 다름 → **해결** (2026-09-15 구두 · 시안 `5295:83774` 확인분)

앞 세션은 Figma MCP 가 다른 계정(개인)으로 붙어 있어 시안을 못 읽고 보류했다.
계정을 `s1designux` 로 다시 인증하고 **`5295:84026`(Result Title)** 을 읽어 대조했다.

시안의 오른쪽 묶음은 **`[ic_이미지 18 ─슬라이더 96─ ic_이미지 24]` · `[Select 140×34]` · `[`썸네일`|`원본` 34]`** 순서이고, 묶음 사이 간격은 20px · 전체 폭 434 이다.

| 자리 | 종전 | 시안대로 고친 것 | 사양서 수정/확인 필요 |
|---|---|---|---|
| **묶음 순서** | 정렬 → 슬라이더 → 버튼 2개 | **슬라이더 → 정렬 → 썸네일/원본** | 변경 없음 |
| 크기 슬라이더 양 끝 | **아이콘이 아니라 CSS 테두리 삼각형**(`.size-slider i`) | `ic_이미지`(= 기존 `image.svg`, 글리프 동일) **작게 18 / 크게 24**, 간격 2 | 변경 없음 |
| 슬라이더 트랙 | 단색 2px · 핸들 12px(어두운 바탕 + 2px 테두리) | **왼쪽 채움**(`#b8babf`) + 나머지 `#2e2f38` · 핸들 **10px `#c4c4c4` + 흰 1px + 그림자** | 변경 없음 |
| 정렬 Select Box | 140×**24** · 12px · 좌우 7 · chevron **20** | 140×**34** · **14px Regular** · 좌 16 / 우 8 · chevron **24** | 변경 없음 |
| 우측 버튼 2개 | **아이콘 버튼** `.i-tool-multi` · `.i-tool-expand` (38×26, 4px 떨어짐, 파랑 틴트) | **글자 토글 `썸네일` \| `원본`** — 각 56×34, 붙여서 바깥 모서리만 r4, 선택 = `#3070d8` 채움 + 흰 글자 | 사양서에 보기 전환이 **아이콘**으로 적혀 있으면 **글자 토글**로 고쳐야 한다 |

- 시안의 드롭다운 화살표 글리프 자체는 기존 `chevron.svg` 와 **같은 path** 였다(`M10.375 7.75L14.625 12L10.375 16.25`). 달랐던 건 **크기(16→24)** 뿐이라 SVG 추가 없이 클래스만 `i-24` 로 올렸다.
- `.size-slider` 는 **영상 선택 팝업(`#clipsSize`)도 같은 규칙**을 쓴다. 시안 팝업(`5295:84091`)도 같은 `ic_이미지` 18/24 라 함께 바꿨다. 팝업의 Select Box(시안 108×34)·`가로/세로` 아이콘 버튼은 이번 범위 밖 — **다음 청크에서 대조 필요**.
- 트랙 채움은 `::-webkit-slider-runnable-track` 을 직접 그리면 `accent-color` 가 죽으므로, 채움 비율을 `--pct` CSS 변수로 넘긴다(`syncSliderFill`). 팝업처럼 나중에 그려지는 슬라이더는 MutationObserver 로 따라간다.

확인(Chromium 1920×1000, CDP) : 오른쪽 묶음 434×34 · 슬라이더 142(아이콘 18 + 간격 2 + 트랙 96 + 아이콘 24) · Select 140×34 · caret 24 · 토글 56+56=112 · 선택 `rgb(48,112,216)`/흰 글자 ↔ 비선택 `rgb(28,29,35)`/`rgb(184,186,191)` · 묶음 x 오프셋 0 / 162 / 322 (시안과 동일) · 팝업 슬라이더도 아이콘 18·24 + `--pct` 30.77% 적용 · 페이지 오류 없음

> **도구 메모** — 이 PC(Windows)에는 Playwright 가 없고 기존 `tools/qa/*.js` 는 macOS 경로로 고정돼 돌지 않는다.
> Playwright 없이 Chrome 원격 디버깅에 직접 붙는 **`tools/qa/_cdp.js`** 를 새로 넣었다(`tools/qa/capsort.js` 가 사용 예).

## 검색 홈 전체를 시안 `5295:84013` 에 맞춤 — 정렬 줄 외 5건 (2026-09-15 구두)

정렬 줄을 고친 뒤 검색 홈 전체를 시안과 대조했다. 아래 5건을 사용자 확인을 받아 반영했다.

| 항목 | 시안 | 종전 목업 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|---|---|
| **필터 아코디언 순서** | 카메라 → **기간 → 유사도** → 색상 | 카메라 → **유사도 → 기간** → 색상 | **시안대로 교체**(`FILTER_DEF` 4개 방식 모두) | **231행의 `카메라 · 유사도 · 기간 · 색상` 확정이 뒤집혔다.** 와이어프레임 4862:108451 보다 260915 시안이 최신 — 사양서도 `기간 · 유사도` 순으로 |
| **카드 크기·열 수** | 208.43 × 236, **7열**, 간격 8 | 264 × 270, **5열**, 간격 12 | 기본 폭 **208** · 간격 **8** → 1507 안에 7열 | 카드 높이는 236(시안) ↔ 224(목업) 로 12 차 — 썸네일 비율 문제, **확인 필요** |
| **머리 줄 ↔ 카드 간격** | 12 | 32 | **12** | 변경 없음 |
| **검색어 입력 우측** | **돋보기**(누르면 검색) | ✕(지우기)만 | 돋보기 추가(`#qTextGo` → `#btnSearchGo`). **✕ 는 글자가 있을 때만 그 왼쪽에 유지** | 시안에는 ✕ 가 없다. 지우기를 뺄지 **확인 필요**(빼면 초기화 버튼으로만 비운다) |
| **패널·결과 안쪽 배치** | 패널 내용 84~376(292) · 아코디언 88~372(284) · **구분선 386** · 결과 397~1904(1507) | 패널 내용 88~384 · 구분선이 결과 왼쪽 끝(396) · 결과 412~1896 | 시안 좌표대로. 구분선은 배경 그라디언트 → `#results::before`(left −10) | 변경 없음 |

**그대로 둔 것**

| 항목 | 이유 |
|---|---|
| `결과 더보기 ⓘ` · `총 [30] 건` 숫자 강조 | 시안엔 없지만 **2026-09-15 구두로 유지 결정**. 243행 확정 유지 |
| 헤더 `AI` 버튼 | 시안엔 있지만 **2026-08-31 "AI 기능 제외" 구두 지시**(371행) 유지 |
| 검색 패널 면 색 `#1c1d23` | 시안은 `#131418`. **2026-09-15 "GNB 카드 배경을 검색 패널과 같은 톤으로"** 구두 지시(620행) 우선 |
| 탭 줄에 탭 3개 · `검색` 버튼 활성 | 시안이 다른 **상태**를 그린 것으로 보여 차이로 치지 않음 |

**회귀 하나와 그 처리** — 패널 내용을 12 좁히자 모드 칩(총 291)이 284 안에 안 들어가 두 줄로 밀렸다.
시안은 칩 줄이 아코디언보다 4씩 넓은 292(84~376)이고 칩 간격도 4다. 칩·입력을 292로 넓히고 간격을 6→4로 줄여 한 줄로 되돌렸다.

확인(Chromium 1920×1080, CDP) : 카드 `208×7열` 간격 8 · 첫 카드 x 396 y 133(머리 줄 아래 12) · 아코디언 88~372(284) · 칩 5개 모두 y 86 한 줄(84~369) · 입력창 84~376(292) · 아코디언 순서 `카메라(위치) · 기간 · 유사도 · 색상` · 결과 30건 · 페이지 오류 없음

## 검색 홈 후속 보정 4건 + 상세 탭 글자색 (2026-09-15 구두)

| # | 지적 | 원인 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|---|---|
| 1 | 구분선이 **탭 끝 선과 안 맞음** | 앞 청크에서 구분선만 396 → 386 으로 옮기고 **패널 카드는 396 에 그대로** 뒀다. 시안은 `y=57` 을 훑으면 **386~394 가 바탕색 `#0d0e12`** — 즉 패널 카드가 386 에서 끝나고 탭은 394 부터다 | 패널 카드 **320 → 310**(76~386) · 구분선을 카드 경계(386)로 · 결과 내용 **397~1904** · 탭 시작 **394** | 변경 없음 |
| 2 | **카메라 트리 영역 배경** 없음 | `.acc-body` 가 투명이었다. 시안은 `select` 프레임 284×314 에 **`#131418`** | 카메라 아코디언 본문에 `--bg-1` + 모서리 4 + 안쪽 여백 10 | 다른 아코디언(기간·유사도·색상)은 시안에도 박스가 없어 그대로 뒀다 — **확인 필요** |
| 3 | **버튼 배경 컬러 안 맞음** | `.side-foot{background:var(--bg-1)}` 라 버튼 줄만 `#131418`. 시안은 카드와 같은 **`#1c1d23`** | 배경 투명(카드 면 노출) · 버튼 **142×34** 두 개, 간격 8, 84~376 | 변경 없음 |
| 4 | **썸네일 크기 슬라이더가 또 안 먹음** | 원본 보기에서 `render()` 가 돌면 `applyResultView` 가 `#resultsBody` 에 **`.zoomed`** 를 붙이는데, `.results-body.zoomed .grid{--thumb-w:320px}` 가 **`.grid` 에 직접** 변수를 박아 `#resultsBody` 의 인라인 값을 이겼다. 끝까지 끌어도 카드가 320 고정 | `.zoomed .grid` 의 `--thumb-w` 를 **`inherit`** 로 되돌림. 크기는 슬라이더, 비율은 썸네일/원본 토글이 맡는다 | 사양서에 `원본 보기 = 3열 확대` 같은 기술이 있으면 **삭제** 필요 |
| 5 | 상세 **선택/비선택 탭 글자색이 같음** | `body.in-detail … .tt-n{color:--tx-primary}`(6775행)이 `.on` 구분 규칙(6912행)보다 **우선순위가 높아** 전부 덮었다 | 선택 `#ecedf0` Medium / `.tt-c` `#b8babf` ↔ 비선택 둘 다 `#8a8c96` Regular | 2026-09-15 확정(575행) 복구 — 변경 없음 |

확인(Chromium 1920×1080, CDP) :
- 패널 카드 76~386 · 접기 버튼 356~376 · 탭 시작 394 · `y=57` 에서 386 부터 `#0d0e12` (시안과 동일)
- 카메라 트리 88~372 = `#131418` · 구분선 386 · 첫 카드 397
- 버튼 줄 985~1040 = `#1c1d23`(종전 `#131418`) · 초기화 84~226 · 검색 235~376 · 높이 34
- `.zoomed` 를 강제한 상태에서 슬라이더 끝까지 끌기 → 카드 320 → **140** (종전엔 320 고정)
- 상세 탭 선택 `rgb(236,237,240)`/500 ↔ 비선택 `rgb(138,140,150)`/400
- 페이지 오류 없음

> `tools/qa/_cdp.js` 에 **진짜 마우스 입력**(`p.mouse` · `p.drag`)을 추가했다. range 끌기·카드 더블클릭은 DOM `click()` 으로 재현되지 않아 이게 없으면 확인이 안 된다.

## 영상 비율 : 팝오버 선택 → **버튼 토글** (2026-09-15 구두 · 상세화면 시안 `화면 비율 변경`)

| 항목 | 종전 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|---|
| 조작 방식 | 비율 버튼을 누르면 **팝오버**가 열리고 `원본 비율 / 맞추기 / 자르기` 중 하나를 고른다 | **버튼을 누를 때마다** `원본 비율 → 맞추기 → 자르기 → 원본 비율` 로 돈다 | 사양서의 **팝오버 선택 기술을 토글로 교체** 필요 |
| 아이콘 | 상태와 무관하게 `g-ratio` 하나 | 상태별로 다른 아이콘(시안 `화면 비율 변경` 의 □ · ←→ · ↗↙) — **표(`V_RATIO_IC`)만 만들어 두고 아직 세 상태 모두 `g-ratio`** | **시안 노드를 못 받아 아이콘 교체 보류.** 노드를 받으면 `assets/icons/*.svg` + `build-icons.js` 로 교체 |
| 첫 누름 기준 | `dataset.ratio` 가 비어 있으면 무조건 `원본 비율` 로 쳤는데, 상세 영상은 실제로 `cover`(자르기)로 보이던 터라 **첫 누름이 한 칸 어긋났다** | 화면에 실제로 걸린 `object-fit` 을 읽어 현재 상태로 삼는다(`vRatioCurrent`) | 상세 영상 **기본 비율**이 자르기가 맞는지 확인 필요(시안 첫 항목은 `원본 비율`) |
| 활성 표시 | 기본값이 아닐 때 버튼 `.on` | 그대로 유지 | 토글이면 현재 상태를 아이콘이 알려 주므로 `.on` 강조가 필요한지 확인 필요 |
| 종전 팝오버 | — | **삭제하지 않고 `openVRatioPop()` 으로 남겨 둠**(호출 안 함). CSS `.v-ratio-pop` 도 유지 | 변경 없음 |

확인(Chromium, 상세 화면) : 초기 `cover` → 1번 `원본 비율`/contain → 2번 `맞추기`/fill → 3번 `자르기`/cover → 4번 `원본 비율`/contain, 팝오버 안 뜸, 페이지 오류 없음

## 접힘 탭 위치 · 결과 더보기 → 하단 자동 로드 (2026-09-15 구두)

> ⚠️ **시안 노드 ID 가 통째로 바뀌었다.** 검색 섹션은 `5295:*` → **`5329:*`**
> (섹션 자체는 `5295:83774` 그대로). 이 문서 앞쪽의 `5295:…` 표기는 **옛 ID** 다.
> 새 좌표 : 펼침 `5329:111931` · 접힘 `5329:111727` · 탭 바 `5329:111936`(펼침) / `5329:111732`(접힘)

| 항목 | 시안 | 종전 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|---|---|
| 접힘 `검색 결과` 탭 위치 | x **76** (Container 왼쪽 끝) | x **84** — 펼침용으로 넣은 `.tabbar{padding-left:8}` 이 접힘에도 걸렸다(내가 만든 회귀) | 접힘일 때 `padding-left:0` | 변경 없음 |
| 탭 폭 | 펼침 : `검색 결과` **310**(= 패널 폭) + 나머지 각 **200** / 접힘 : 11개일 때 **164.7 + 156.7×10** 로 1732 를 꽉 채운다 | **140 고정**(2026-09-15 확정) | **미반영 — 확인 대기** | 시안은 "**200 기본, 넘치면 균등 축소**" 로 보인다. 140 고정 확정을 바꿀지 **확인 필요** |
| `결과 더보기 ⓘ` | (접힘 프레임에는 아직 있다) | 머리 줄 `총 N 건` 옆 버튼 | **버튼 제거**(CSS `.rh-more`·`.rh-info` 는 아카이브). 목록 **하단**에 상태 줄을 두고 **끝에 닿으면 자동 로드** | 사양서의 `결과 더보기 버튼` 기술을 **무한 스크롤**로 교체 필요 |
| 하단 상태 줄 | — | — | 아직 안 부름 = **아무것도 안 보임** / 부르는 중 = 스피너 + `결과를 더 불러오는 중…` / 끝 = `모든 결과를 불러왔습니다.` | 문구·표시 방식 확인 필요(임의) |
| 결과가 적어 스크롤이 없을 때 | — | — | 기다릴 것 없이 **바로 채운다**(안 그러면 영원히 못 부른다) | 변경 없음 |
| 불러오는 범위 | — | 유사도 기준 **-15%p**, 한 단계뿐 | 그대로(종전 `S.moreLoaded` 한 단계) | **여러 번 이어서 불러올지** 확인 필요 — 지금은 한 번이면 끝이다 |

확인(Chromium) :
- 접힘 `검색 결과` 탭 x **76**(종전 84) · 폭 140 · 높이 28
- 결과 30건 상태에서 머리 줄에 더보기 버튼 없음 · 하단 상태 줄도 없음 → **끝까지 스크롤하니 34건으로 자동 증가** + `모든 결과를 불러왔습니다.` · 다시 스크롤해도 중복 호출 없음
- 결과 5건(스크롤 없음) : 바로 채우고 `모든 결과를 불러왔습니다.`
- 페이지 오류 없음

## 영상 선택 팝업 자동 로드 · 작은 칸 아이콘 모두 노출 (2026-09-15 구두)

| 항목 | 종전 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|---|
| **영상 선택 팝업 더보기** | 목록 하단 `더보기 (N개 남음)` 버튼 + `한 번에 N개까지 불러옵니다.` 안내 | **끝에 닿으면 자동으로 이어 붙인다**(`bindClipAutoMore`). 검색 결과 목록과 같은 방식. 버튼 마크업은 주석으로 아카이브, CSS `.pk-more` 유지 | 사양서의 `더보기 버튼` 기술을 **무한 스크롤**로 교체 필요 |
| 하단 상태 줄(팝업) | — | 아직 남았는데 안 부름 = 안 보임 / 부르는 중 = 스피너 + `영상을 더 불러오는 중…` / 끝 = `모든 영상을 불러왔습니다.`(처음부터 한 화면에 다 들어오면 아무것도 안 띄운다) | 문구 확인 필요(임의) |
| 다시 그릴 때 스크롤 | — | 검색 결과·영상 선택 **둘 다** `innerHTML` 을 통째로 갈아 끼워 스크롤이 맨 위로 튄다 → **보던 위치를 지킨다** | 변경 없음 |
| **경로 비교 작은 칸 아이콘** | 3·4명(`slots >= 3`)이면 머리말 아이콘을 **더보기(⋯) 하나로 접었다** — 북마크·비율·확대가 메뉴 안에 숨었다 | **칸이 작아도 세 아이콘을 모두 노출**(⋯ 제거) | 사양서에 `칸이 작으면 ⋯ 로 접는다` 기술이 있으면 삭제 필요 |
| 작은 칸 비율 버튼 | ⋯ 메뉴의 `영상 비율` 항목 | 머리말 비율 아이콘이 **같은 토글**(원본 비율 → 맞추기 → 자르기)로 동작 | 변경 없음 |

확인(Chromium) :
- 영상 선택 팝업 : 카드 12 → 끝 스크롤 → **24 → 35** → `모든 영상을 불러왔습니다.` · 더보기 버튼 없음 · 중복 호출 없음
- 검색 결과 : 30 → 끝 스크롤 → **34** → `모든 결과를 불러왔습니다.`
- 경로 비교 4명 : 칸마다 `북마크 · 영상 비율 · 영상 확대` **3개 모두** 노출, `⋯` 없음
- 페이지 오류 없음

## 영상 비율 토글 — 시안 아이콘 3종 반영 (2026-09-16 구두 · 시안 5318:87771 `변경된 아이콘`)

토글 동작은 이미 3단계로 돌고 있었는데 **아이콘은 세 상태 모두 같은 `g-ratio`** 를 쓰고 있었다.
시안 프레임에서 SVG 를 받아 갈라 끼웠다.

| 상태 | 시안 노드 | 아이콘 파일 | object-fit |
|---|---|---|---|
| 원본 비율 | `ic_원본비율` 5318:87772 | `g-ratio-original.svg` | `contain` |
| 맞추기 | `ic_맞추기` 5318:87786 | `g-ratio-fit.svg` | `fill` |
| 자르기 | `ic_자르기` 5318:87781 | `g-ratio-crop.svg` | `cover` |

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 아이콘 추출 | 프레임 export 에 캔버스 배경(`#222222` 사각형 + 섹션 바탕)이 딸려 와서 `<g id="ic_…">` 서브트리만 남기고 다시 감쌌다. 벡터 데이터는 손대지 않음 | 변경 없음 |
| 버튼 초기 상태 | 마크업은 `원본 비율` 인데 CSS 기본이 `cover`(= 자르기) 라 **첫 누름에서 아이콘이 안 바뀐 것처럼** 보였다 → 화면에 실제로 걸린 `object-fit` 을 읽어 버튼을 맞춘다(`syncVRatioAll`, 상세 렌더 때 호출) | 단일 영상의 기본 표시가 `자르기`(꽉 채움) 가 맞는지 확인 필요 — 맞다면 사양의 기본값 표기를 그렇게 고쳐야 한다 |
| 활성(on) 강조 | 상태마다 아이콘이 다르므로 **강조는 쓰지 않는다**. 종전 규칙(`기본값이 아닐 때만 on`)은 기본이 자르기라 처음부터 켜져 보였다 | 변경 없음 |

확인(Chromium, 상세 화면) : 초기 `자르기`/cover → 누름 `원본 비율`/contain → `맞추기`/fill → `자르기`/cover 로 한 바퀴. 아이콘 마스크 3종이 서로 다르고 한 바퀴 뒤 처음과 일치. 페이지 오류 없음

## 카메라 칩의 `ⓘ` 삭제 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| `N개 선택 ✕ ⓘ` | `ⓘ` 를 뺀다. `✕`(선택 해제)만 남는다 | 사양의 "5개 이상 선택 시 검색 지연 안내" 표시 방법 재정의 필요 — 안내 문구 자체를 없앨지, 다른 자리로 옮길지 |
| 코드 | 생성 코드는 남기고 호출만 끊었다(`slow = false && …`) — 되살릴 땐 조건만 되돌리면 된다 | 변경 없음 |

확인(Chromium) : 카메라 7개 선택 상태에서 칩 = `7개 선택✕`, `.acc-warn` 0개, `✕` 유지

## 확대(돋보기) 개념 변경 · 칸별 확대는 더블클릭 · 멀티뷰 머리말 버튼 노출 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 확대 버튼 | 종전 `1.0 / 2x / 3x / 4x` **배율 팝오버** → **지금 보고 있는 화면을 큰 팝업으로**. 분할 수와 무관하며 1분할이면 영상 하나, 2·3·4분할이면 **채널 수 그대로** 배치를 유지해 띄운다 | 사양의 `영상 확대 = 배율 선택` 을 `현재 화면 전체 보기` 로 고쳐야 한다 |
| 칸별 확대 | **칸을 두 번 클릭** → 1.0 → 2x → 3x → 4x → 1.0. 휠 확대와 같은 대상·단계 | 사양에 더블클릭 확대 추가 필요 |
| 두 번 클릭 판정 | 영상 칸에서는 네이티브 `dblclick` 이 문서까지 오지 않아(칸 쪽에서 먼저 먹는다) **클릭 수를 직접 센다**(400ms). 첫 클릭의 기존 동작은 그대로 두고 두 번째에 확대만 올린다 | 변경 없음 |
| 멀티 뷰 칸 머리말 | 종전에는 `영상 위치 전환(dt-swap)` 일 때만 3개를 펴고 기본 배치에서는 `⋯` 하나였다 → **분할 수·배치와 무관하게 북마크·비율·확대 3개 노출, `⋯` 숨김** (경로 비교 칸과 동일) | 사양의 "넓은 영역에서는 ⋯ 하나" 표기 삭제 필요 |
| 팝업 만드는 법 | 데이터가 아니라 **화면에 깔린 칸을 DOM 에서 그대로 걷어** 구성한다(`stageTiles`) — 멀티 뷰든 경로 비교든 같은 경로 | 변경 없음 |

확인(Chromium) : 1분할 → 단일 스테이지 팝업 · 2/3/4분할 → 팝업 칸 2/3/4 개 · 칸 머리말 버튼 3개 노출·`⋯` 숨김 · 칸 두 번 클릭 → `1.0 → 2x → 3x` · 페이지 오류 없음

## 화면 분할 버튼 하나로 합치기 · 재생 영상 콤보박스 분리 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 분할 버튼 | 종전 `[1분할] [4분할 ▾]` **두 버튼 + 팝오버** → **버튼 하나**. 누를 때마다 `1 → 2 → 3 → 4 → 1` 순차 전환 | 사양의 `View Mode = 1Viewer / 4Viewer + 팝오버` 를 `버튼 하나 순차 전환` 으로 고쳐야 한다 |
| 현재 분할 표시 | 아이콘 우하단 **숫자 배지**(1/2/3/4). 1분할은 `g-view1`, 2분할 이상은 `g-view4` 아이콘 | 2·3분할 전용 아이콘이 시안에 있으면 받아서 교체 필요(지금은 4분할 아이콘 공용) |
| 재생 영상 | 팝오버 안에 있던 `이동 경로 / 주변 카메라` 를 **옆 콤보박스**(`#dtMvType`)로 분리. 1분할에서는 쓸 일이 없어 흐리게(비활성) | 변경 없음 |
| 종전 마크업 | `.vm1` · `#dtViewSel`(팝오버 포함) 은 **지우지 않고 CSS 로 숨겼다** — 되돌릴 때 그 블록만 지우면 된다 | 변경 없음 |
| 경로 비교 툴바 | 이번에는 **상세 화면만** 바꿨다. 경로 비교(`#cmpCtrl`)의 `[단일] [멀티 뷰 ▾]` 는 그대로 | 비교 툴바도 같은 방식으로 합칠지 확인 필요 |

확인(Chromium) : 초기 배지 `1`·단일 → 누를 때마다 배지 `2/3/4` 와 실제 칸 수 `2/3/4` 가 일치 → 한 바퀴 뒤 다시 `1`·단일 · 콤보박스에서 `주변 카메라` 선택 시 값·화면 반영 · 옛 버튼 둘 다 숨김 · 페이지 오류 없음

## 영상 선택 팝업 — `재검색` 활성 조건 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 활성 조건 | **기간(날짜) 또는 `전체 카메라에서 검색` 체크가 바뀐 때만** 활성. 종전에는 늘 눌린 상태였다 | 사양에 재검색 활성 조건 명시 필요 |
| 비활성 조건 | 팝업을 연 직후 · 이름 입력 · 정렬/크기/보기 변경 · 재검색을 누른 직후. 바꿨다가 **원래 값으로 되돌리면 다시 비활성** | 변경 없음 |
| 기준값 | `REID.appliedAllCam` · `REID.appliedPeriod` = 지금 화면에 걸린 조건. 팝업을 열 때와 재검색 직후에 갱신한다 | 변경 없음 |

확인(Chromium) : 연 직후 비활성 → 이름 입력 비활성 → 정렬 변경 비활성 → 기간 `최근 7일` 로 변경 **활성** → 재검색 누른 뒤 비활성 → 전체 카메라 해제 **활성** → 되돌리면 비활성 · 페이지 오류 없음

## 영상 선택 팝업 — 시안 대조 (2026-09-16 구두 · 시안 5329:111961)

구현 화면과 시안이 달라 맞췄다. **앞선 구두 지시와 충돌하지 않는 것만** 반영하고, 충돌 3건은 그대로 뒀다(사용자 판단 : "안전한 것만 먼저").

| 항목 | 종전 | 시안대로 |
|---|---|---|
| 사진 `＋` 버튼 | 우상단 | **우하단** |
| 이름 안내 문구 | 입력 아래 **상시 노출**(`.vs-hint`) | `ⓘ` **호버 툴팁** (`data-tip`) |
| `기간` 라벨 | 옆에 값 배지 `[오늘]` | 배지 없음 — 고른 라디오가 곧 값 |
| 보기 전환 | 아이콘 2개(가로/세로) | **`썸네일` / `원본` 글자 세그먼트** |
| `재검색` 버튼 | 늘 파란색 | 조건이 바뀌기 전에는 **회색(비활성)** |

### 시안과 다르지만 **그대로 둔 것** (앞선 구두 지시 우선 — 되돌릴지 확인 필요)

| 지금 | 근거 | 시안 |
|---|---|---|
| 날짜 디바이더 + 날짜별 `전체 선택` | 2026-09-01 4차 요청 | 디바이더 없는 평면 그리드 + 좌상단 `전체 선택` 하나 |
| 좌상단 `n/전체` 선택 배지 | 2026-09-10 요청 | `총 N건` 표기 |
| 하단 `이동 경로 미리보기` 띠 | 2026-09-01 4차 요청 | `영상을 선택해 주세요.` 안내 + 가운데 확대 슬라이더 |

## 재생 영상 콤보박스를 위로 펼치기 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 펼침 방향 | 조작 바가 화면 아래라 아래로 펼치면 타임라인에 가려 잘렸다 → **위로** 펼친다(캐럿 방향도 반대로) | 변경 없음 |

확인(Chromium) : `＋` 사진 아래쪽 · 상시 안내문구 없음 · 툴팁 속성 있음 · 기간 배지 없음 · 세그 `썸네일`/`원본` · 재검색 비활성 배경 `rgb(36,37,44)` · 콤보박스 메뉴 바닥(798) ≤ 버튼 위(801) 로 위로 열리고 화면 안에 들어옴 · 페이지 오류 없음

## 분석 도구를 조작 바 왼쪽으로 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 자리 | 히트맵 · 주변 카메라 보기가 조작 바 **오른쪽**(`#dtTools`)에 있던 것을 **왼쪽**(`#dtAreaTools`)으로 옮겼다 | 사양서 2-5) 영상 분석·뷰어 도구의 배치를 왼쪽으로 고쳐야 한다 |
| 순서 | **영역 검색 · 가상펜스 · 히트맵 · 주변 카메라 보기** | 변경 없음 |
| 이름 | `인접 카메라` → **`주변 카메라 보기`** | 사양서 명칭 통일 필요 |
| 오른쪽에 남는 것 | 화면 분할 버튼 · 재생 영상 콤보박스 · 전체보기 (경로 확인은 시안에 없어 계속 숨김) | 변경 없음 |

확인(Chromium) : 왼쪽 순서 `영역 검색 · 가상펜스 검색 · 히트맵 · 주변 카메라 보기`, x = 20 / 56 / 92 / 128 (36px 간격), 모두 조작 바 왼편 · 페이지 오류 없음

## 영상 선택 카드 비율 (2026-09-16 구두 · 시안 5329:111961 실측)

카드가 시안보다 세로로 길어 보이던 원인은 **이미지 비율**이었다.

| 항목 | 종전 | 시안 실측 | 반영 |
|---|---|---|---|
| 카드 이미지 비율 | `aspect-ratio:3/4` = **0.75:1** (세로로 김) | 194.6 × 176 = **1.106:1** | `aspect-ratio:194.6/176` |
| 캡션 안쪽 여백 | 7/8/9 | 상하 8 · 좌 10 · 우 4 | 시안대로 |
| 캡션 글자 | 제목 12px 600 · 부제 10px mono | 제목 **14px Medium**(자간 -0.28) · 부제 **12px Regular** | 시안대로 |

확인(Chromium) : 이미지 비율 **1.106** (시안과 동일) · 한 줄 **5개** (시안과 동일) · 캡션 높이 50(시안 53)

### 남은 차이 — 카드 **크기**

카드가 `160 × 194`, 시안은 `194.6 × 228` 이다. 비율과 열 수(5개)는 같고 **팝업 폭이 좁아** 비례 축소된 것이다.
시안 그리드 폭 1015 ↔ 구현 약 872 (약 143px 차이). 크기까지 맞추려면 **팝업 폭을 시안(1282)으로 넓혀야** 한다. — 확인 필요

## 영상 두 번 클릭 = 크게 보기 (2026-09-16 구두 · 앞선 지시 수정)

같은 날 오전에 `두 번 클릭 = 배율 확대` 로 넣었던 것을 **크게 보기(팝업)** 로 바꿨다.

| 조작 | 동작 |
|---|---|
| 확대(돋보기) 버튼 | 지금 보고 있는 **화면 전체**를 팝업 (1분할이면 영상 하나, 2·3·4분할이면 배치 그대로) |
| 영상 **두 번 클릭** | **그 칸 하나**를 크게 보기 팝업 |
| **휠** | 배율 확대/축소 (1.0 → 2x → 3x → 4x) — 그대로 |

확인(Chromium) : 1분할 두 번 클릭 → 단일 스테이지 팝업 · 2분할 칸 두 번 클릭 → 단일 스테이지 팝업(배율 1.0 그대로) · 휠 올림 → 칸 배율 2x · 페이지 오류 없음

## 조작 바 순서 · 유사도순 평면 · 건수 표기 · 미리보기 확대 (2026-09-16 구두)

| 항목 | 종전 | 반영 |
|---|---|---|
| 조작 바 오른쪽 순서 | 뷰 모드 → 콤보박스 → 확대 | **콤보박스 → 뷰 모드 → 확대(전체보기)** |
| 영상 선택 `유사도순` | 유사도 구간(`95% 이상`·`90~94%` …)으로 묶어 디바이더 표시 | **디바이더 없이 유사도 높은 순으로만** 평면 배치. 최신순·위치순의 묶음은 그대로 |
| `총 N 건` 숫자 | 파란 테두리 + 파란 글자 + 모노 폰트 배지 | **테두리·색 없이 본문 글자**(Pretendard Medium) |
| 미리보기 확대/축소 슬라이더 | `accent-color` 만 줘 브라우저 기본 굵은 파란 막대 | 상세 타임라인과 같은 규격 — **트랙 88×3 · 손잡이 11px 흰 원** |
| 미리보기 확대 | 구간(간격)만 벌어지고 썸네일은 그대로 | **썸네일도 함께 커진다** (`--cpz` 배율, 간격도 같이 따라감) |

확인(Chromium) : 오른쪽 순서 `dtMvType(1102) → dtSplitBtn(1212) → dtFull(1248)` · 유사도순 디바이더 0개/그리드 1개(최신순은 디바이더 2개 유지) · 건수 테두리 `0px`·색 `rgb(236,237,240)` · 슬라이더 `88×3px`·트랙 `rgb(62,64,73)` · 썸네일 `72×46 → 144×92 → 216×138` · 페이지 오류 없음

## 남은 건 일괄 처리 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| **미리보기 패널이 상세에 뜨던 오류** | 원본 영상 미리보기(`#preview`)는 **검색 홈 전용**이다. 상세에서 `renderPreview` 가 어느 경로로 불려도 안 뜨도록 JS·CSS 양쪽에서 막았다. (호출 경로 재현은 안 됐고, 불변조건으로 처리) | 변경 없음 |
| 조작 바 오른쪽 | 콤보박스 왼쪽 **구분선 제거**, 요소 간격을 **8px 한 값**으로 통일 | 변경 없음 |
| 영상 선택 팝업 폭 | 1138 → **1282**(시안). 카드 기본 폭 150 → **180** 으로 맞춰 **한 줄 5개** | 변경 없음 |
| 재생 영상 콤보박스 화살표 | 아래 방향 하나 → **위·아래 양방향** (디자인 시스템 chevron 을 겹쳐 씀) | 변경 없음 |
| 이동 경로 보기 순번 | 칸 **좌하단 배지** → 머리말 **카메라명 앞**으로 (좌하단은 숨김, 코드는 남김) | 사양에 순번 위치 명시 필요 |
| 조작 바 양끝 버튼 | 동작이 없던 `이전/다음 영상` → **클립(구간) 건너뛰기**. 커서를 앞/뒤 구간 **시작 지점**으로 옮긴다 | 사양서 2-4) `이전/다음 영상` 을 구간 건너뛰기로 정의 필요 |
| 양끝 버튼 활성 조건 | `DT.clip` 기준이라 첫 구간에서 `이전`이 늘 꺼져 있었다 → **타임라인 구간이 2개 이상이면 활성** | 변경 없음 |

확인(Chromium) : 팝업 1282 · 카드 188×220 · 한 줄 5개(시안 194.6×228 / 5개) · 조작 바 요소 `dtMvType → dtSplitBtn → dtFull` 간격 8/8 · 구분선 없음 · 화살표 2개 · 머리말 순번 `1` 이 이름 앞 · 좌하단 배지 숨김 · 양끝 버튼 활성, 커서 0.0098 → 0.1184 → 0.139 → (이전) 0.1184 · `renderPreview` 강제 호출해도 상세에서 패널 숨김 · 페이지 오류 없음

## 검색 패널을 시안 실측으로 (2026-09-16 구두 · 시안 5391:119185 `검색_진입`)

| 항목 | 종전 | 시안 실측 → 반영 |
|---|---|---|
| 검색 입력 | 여러 줄 · 우측 돋보기 버튼 · `검색어를 입력해 주세요.` / `(예: 흰색 옷을 입고 가방을 든 사람)` | **h44 · 모서리 8 · 안쪽 12 · 14px Regular(-0.28) · 돋보기 없음** · 문구 **`검색 대상의 특징을 문장으로 입력해 보세요.`** |
| 카메라 검색창 | 트리 상자 **안** 상단 · `카메라 검색` · 작은 아이콘 | 트리 상자 **바깥 위** · h34 · 모서리 4 · 왼 12/오른 8 · **아이콘 24** · 문구 **`검색`** |
| 트리 행 구성 | `화살표 → 체크박스 → 폴더 → 이름` | **`체크박스(18) → 화살표(20) → 폴더(20) → 이름 … 개수`** |
| 2뎁스 목록 | 들여쓰기 없음 | **왼쪽 16 들여쓰기**, 행 높이 32 · 안쪽 좌우 8 |
| 글자 | 이름 11.5px · 개수 10px mono | `전체` **14px Medium** · 항목 이름 **12px Regular** · 개수 **12px Regular 50%** · 아코디언 제목 **12px Medium** |
| 라벨 | `카메라(위치)` | **`카메라 (위치)`** (괄호 앞 공백) |

확인(Chromium) : 검색 입력 44 / radius 8 / 14px / 문구 일치 · 돋보기 버튼 없음 · 카메라 검색 34, 상자 **밖**, 아이콘 24, 14px · `전체` 행 32/14px · 목록 들여쓰기 16px · 행 32, 순서 `[input.cb, span.ct-cv, svg.ic, span.ct-nm, span.ct-n]` · 이름 12px · 개수 12px/0.5 · 페이지 오류 없음

> 패널 폭은 시안 292, 구현 320 으로 다르다 — 지시에 없어 건드리지 않았다. **확인 필요**
> 종전 시안(5295:84013)이 넣으라고 했던 **입력창 우측 돋보기**는 이번 시안에 없어 뺐다. 되살릴 땐 `#qTextGo` 마크업만 복구하면 된다(핸들러는 남아 있다).

### 검색 패널 마무리 (2026-09-16 구두)

| 항목 | 반영 |
|---|---|
| 패널 접기 버튼 | 시안 `ic_패널접기`(5391 Search Home) SVG 를 받아 `g-panel-fold2` 로 교체, 20px |
| 카메라 검색 아이콘 | 시안 `ic_찾기/조회` SVG 를 받아 `g-find` 로 교체, 24px |
| 체크박스 | 트리·`전체` 모두 **18px 한 값**으로 고정(커 보이던 것 정리) |
| 검색창 테두리 | 포커스 때만 파랗던 것 → **기본 상태에서도 파란 테두리**(시안 `line/blue`) |
| 세로 정렬 | 행 높이 32 안에서 체크·화살표·폴더·글자 모두 가운데 정렬 |

## 히스토리를 버튼 아래 패널로 (2026-09-16 구두 · 시안 5391:119198)

| 항목 | 종전 | 반영 |
|---|---|---|
| 형태 | 화면 가운데 **모달** + 딤 | **히스토리 버튼 바로 아래에 붙는 패널**. 딤 없음 |
| 크기·자리 | `md-sm` 460 가운데 | **440 폭**, 버튼 오른쪽 끝에 맞춰 우측 정렬, 높이는 화면에 맞춰(시안 564) |
| 닫기 | ✕ · 딤 클릭 | ✕ · **버튼 재클릭** · **바깥 클릭** · **Esc** |

확인(Chromium) : 폭 440 · 버튼 아래 · 우측 정렬 차 0 · 딤 없음 · 버튼 재클릭/바깥 클릭 모두 닫힘 · 페이지 오류 없음

## 미리보기 썸네일 → 위 목록 포커스 (2026-09-16 구두)

| 항목 | 반영 | 사양서 수정/확인 필요 |
|---|---|---|
| 동작 | 하단 `이동 경로 미리보기` 의 썸네일을 누르면 **위 목록에서 그 클립을 포커스**하고 보이도록 스크롤한다. `✕`(빼기)를 누른 경우는 제외 | 사양에 미리보기 ↔ 목록 연동 추가 필요 |
| 강조 | 이미 선택된 카드의 파란 테두리(**1px**)보다 **두꺼운 2px** + 바깥 링(2px). 한 번에 한 장만 | 변경 없음 |
| 미리보기 쪽 표시 | 지금 누른 칸도 테두리로 표시 | 변경 없음 |
| 목록에 없을 때 | 정렬·필터로 목록에 없으면 토스트로 알린다 | 변경 없음 |

확인(Chromium) : 클릭 전 포커스 0 · 선택 카드 테두리 `1px` → 미리보기 3번째 칸 클릭 → 포커스 1장 · 테두리 `2px` · 바깥 링 `0 0 0 2px` · 누른 칸 id 와 포커스 카드 id 가 `o02` 로 일치 · 페이지 오류 없음

## 영상 선택 팝업 마무리 4건 (2026-09-16 구두)

| 항목 | 원인·종전 | 반영 |
|---|---|---|
| 검색창 테두리가 **두 겹** | 감싸는 `.textarea-wrap` 에 이미 테두리가 있는데 `#qText` 에 또 그렸다 | 입력의 테두리를 없애고 **래퍼의 색만** 파랗게. 확대(최근 검색 펼침) 상태에서도 한 겹 |
| `재검색` 비활성 사양 | 내가 따로 칠한 회색(bg-3/helper) | **앱 표준 `.btn-primary:disabled`**(`#1d2735` / `#4a5766`) 을 그대로 쓴다 |
| `날짜 지정` 입력 없음 | 라디오만 있고 입력이 없었다 | 고르면 **시작일·종료일 입력**이 펼쳐진다. 값이 바뀌면 재검색도 활성 |
| 미리보기 확대/축소 | 하단에 슬라이더 | **삭제**(표시만 끔). 썸네일은 영역 높이에 맞춘 **표준 120×76** 한 값 |
| `취소 · 완료` 줄 | 위아래 여백이 컸다 | `8px 16px` 로 줄임(줄 높이 44) |

확인(Chromium) : 래퍼 `1px rgb(48,112,216)` / 입력 `0px`(기본·확대 모두) · 재검색 비활성 배경 `rgb(29,39,53)`·글자 `rgb(74,87,102)` · 날짜 지정 입력 2개 표시 · 확대 슬라이더 숨김 · 썸네일 `120x76` · 푸터 여백 `8px 16px`(높이 44) · 페이지 오류 없음

## 검색 유형 칩 (2026-09-16 구두 · 시안 5391:119185 `Chips`)

칩이 겹쳐 보이던 원인은 테두리가 두 겹이 아니라 **선택 칩에 10% 파란 배경까지 깔려** 있던 것이다.

| 항목 | 종전 | 시안 → 반영 |
|---|---|---|
| 선택 칩 | 파란 선 + **파란 10% 채움** + 밝은 파란 글자(`#7fb0ff`) | 파란 선 + **채움 없음** + 글자도 선과 같은 파랑 |
| 비선택 칩 | 회색 선 | 회색 선 + 회색 글자(`--tx-tertiary`) |
| 규격 | radius 14 | h28 · 좌우 12 · **radius 999** · 12px Medium |

확인(Chromium) : 선택 칩 `57x28` · 테두리 `1px rgb(48,112,216)` · 배경 `rgba(0,0,0,0)` · radius `999px` · 아웃라인/그림자/의사요소 없음

## 검색 패널 다듬기 6건 + 미리보기 2건 (2026-09-16 구두)

| 항목 | 종전 | 반영 |
|---|---|---|
| 차량번호 입력 | 텍스트 입력과 규격이 달랐다 | **텍스트와 동일** — h44 · 모서리 8 · 파란 선 **한 겹**(래퍼에만) · 14px. 문구 **`차량번호(2자리 이상)를 입력해 주세요.`** (시안 5391:119946) |
| `N개 선택` 칩 | `7개 선택 ✕` | **✕ 삭제** (생성 코드는 남기고 호출만 끊음) — 시안에는 ✕ 가 있으나 **구두 지시 우선** |
| 카메라 검색 돋보기 | 3px 아래로 치우침 | 입력 상하 **정중앙**(중앙차 0) |
| 체크박스 | 네이티브(accent-color)와 커스텀 `<i>` 가 섞여 모양·크기가 달랐다 | **모두 같은 커스텀 16×16** (`appearance:none` · 2px 모서리 · 체크/부분선택 직접 그림) |
| 폴더 아이콘 | 라인(외곽선) | 시안 **솔리드** SVG(`g-folder`) 로 교체, 20px |
| 조건 타이틀 | 크기 제각각 | `카메라 (위치)` · `기간` · `유사도` · `색상` 모두 **12px Medium** |
| 미리보기 썸네일 간격 | 썸네일이 120 으로 커지며 서로 **붙었다**(SLOT 84) | 최소 간격 확보 — **SLOT 132**(썸네일 120 + 12) |
| 미리보기 선택 시 목록 포커스 | 2px 라 구분이 약했다 | **3px + 바깥 링 3px** |

확인(Chromium) : 차량번호 래퍼 `1px rgb(48,112,216)`·입력 `0px`·h44·문구 일치 · 칩 `7개 선택`(✕ 0개) · 돋보기 중앙차 `0` · 체크박스 전부 `16x16`·`appearance:none` (9개) · 폴더 아이콘 적용 · 타이틀 `12px 500` · 미리보기 칸 간격 최소 `133` · 포커스 `3px` + 링 · 페이지 오류 없음
